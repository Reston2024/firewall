---
phase: 12-rag-knowledge-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - scripts/rag_query.py
  - scripts/validate-phase12.sh
autonomous: false
requirements:
  - RAG-03
  - RAG-04
must_haves:
  truths:
    - "10 manual security queries against the corpus all return chunks with relevant technical content"
    - "ADR query returns rationale as top-3 chunk — no chunk splits mid-decision boundary"
    - "Foundation-Sec-8B produces a coherent answer with RAG context injected — end-to-end confirmed"
    - "ChromaDB survives reboot with collection count unchanged"
  artifacts:
    - path: "scripts/rag_query.py"
      provides: "Query interface with --llm flag for end-to-end RAG+LLM testing"
      min_lines: 60
    - path: "scripts/validate-phase12.sh"
      provides: "Full validation suite passing all RAG-01 through RAG-04 checks"
      min_lines: 80
  key_links:
    - from: "scripts/rag_query.py"
      to: "http://localhost:11434"
      via: "ChatOllama(base_url='http://localhost:11434')"
      pattern: "ChatOllama.*localhost.*11434"
    - from: "scripts/rag_query.py"
      to: "/var/lib/chromadb"
      via: "Chroma(persist_directory='/var/lib/chromadb')"
      pattern: "persist_directory.*var/lib/chromadb"
---

<objective>
Validate RAG retrieval quality with 10 manual security queries, run the end-to-end RAG+LLM test with Foundation-Sec-8B, and confirm ChromaDB persistence survives reboot.

Purpose: Confirm the knowledge layer produces accurate, contextually relevant results before Phase 13 wires it into the automated triage pipeline. Catch chunking boundary issues and retrieval quality problems now, not during alert triage.

Output: 10-query validation results documented, end-to-end RAG+LLM test passing, reboot persistence confirmed, validate-phase12.sh --full passing all checks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-rag-knowledge-pipeline/12-RESEARCH.md
@.planning/phases/12-rag-knowledge-pipeline/12-01-SUMMARY.md
@.planning/phases/11-foundation-sec-8b-ai-analyst/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run 10 manual security queries and end-to-end RAG+LLM test</name>
  <files>scripts/rag_query.py</files>
  <read_first>
    scripts/rag_query.py
    .planning/phases/12-rag-knowledge-pipeline/12-RESEARCH.md
    .planning/phases/12-rag-knowledge-pipeline/12-01-SUMMARY.md
    .planning/phases/11-foundation-sec-8b-ai-analyst/11-02-SUMMARY.md
  </read_first>
  <action>
This task validates retrieval quality and completes the end-to-end RAG pipeline test.

**Part A: Add --llm flag to rag_query.py for end-to-end testing**

Update scripts/rag_query.py to add a --llm flag that:
- Retrieves k=3 chunks from ChromaDB (same as default)
- Constructs a prompt using the security analyst template from 12-RESEARCH.md Pattern 3
- Sends the prompt to Foundation-Sec-8B via ChatOllama at http://localhost:11434
- Model name: "hf.co/fdtn-ai/Foundation-Sec-8B-Q4_K_M-GGUF"
- temperature=0.1, num_predict=512 (cap at 512 tokens per Phase 11 design)
- Prints the LLM response with source document citations

IMPORTANT: The --llm flag requires temporal separation. The script must:
- Check if Ollama is running: `systemctl is-active ollama`
- If Malcolm containers are consuming too much RAM, print a warning about temporal separation
- Do NOT automatically pause Malcolm — this is a manual test, user controls timing

**Part B: Run 10 security queries and verify retrieval quality**

SSH to supportTAK-server and run these 10 queries using rag_query.py (without --llm, just retrieval):

1. "Why is Ollama bound to localhost only?" — expect ADR-E02 chunk in top-3
2. "What is the SSH hardening configuration?" — expect ssh-management-runbook.md or ADR-0001 chunk
3. "How is Suricata configured for IDS monitoring?" — expect ADR-0007 or suricata-ids-runbook.md chunk
4. "What is the disaster recovery strategy?" — expect ADR-0012 chunk
5. "How are firewall zones configured?" — expect zone-policy-runbook.md chunk
6. "What is the telemetry pipeline architecture?" — expect telemetry-deployment-runbook.md chunk
7. "Why was Guardian chosen over fail2ban?" — expect ADR-0006 chunk
8. "What is the WUI certificate configuration?" — expect ADR-0010 or wui-certificate.md chunk
9. "What is the Foundation-Sec-8B inference throughput on N150?" — expect n150-foundation-sec-8b-benchmark.md chunk
10. "What are the RAM constraints for Malcolm and AI coexistence?" — expect benchmark or REQUIREMENTS.md chunk

For each query:
- Run: `ssh opsadmin@192.168.1.22 "cd ~/Firewall && /opt/rag/bin/python3 scripts/rag_query.py '<query>'"`
- Record: query, top-3 chunk sources, whether relevant content was returned
- A query PASSES if at least 1 of the top-3 chunks contains technically relevant content from the expected source document

If any query fails (irrelevant results):
- Investigate: check chunk boundaries, breadcrumb content, embedding quality
- If a chunking issue: re-index with adjusted parameters
- If an embedding issue: verify model is all-MiniLM-L6-v2 (not a different model)

**Part C: End-to-end RAG+LLM test (RAG-04)**

This requires temporal separation — Foundation-Sec-8B needs ~5.5GB RAM.

1. Ensure Ollama service is running: `ssh opsadmin@192.168.1.22 "systemctl is-active ollama"`
2. Run ONE end-to-end query with --llm flag:
   ```
   ssh opsadmin@192.168.1.22 "cd ~/Firewall && /opt/rag/bin/python3 scripts/rag_query.py --llm 'What security controls protect the Ollama API from unauthorized access?'"
   ```
3. Verify the LLM response:
   - Is non-empty (more than 50 characters)
   - References operational context from the corpus (mentions localhost binding, ADR-E02, or UFW)
   - Does not hallucinate capabilities not in the corpus
4. Wait for model to unload (OLLAMA_KEEP_ALIVE=5m) or manually unload after test

SCP updated rag_query.py to supportTAK-server before running tests.
  </action>
  <verify>
    <automated>ssh opsadmin@192.168.1.22 "cd ~/Firewall && /opt/rag/bin/python3 scripts/rag_query.py 'Why is Ollama bound to localhost?'" 2>&1 | grep -qi "ADR-E02\|localhost\|security"</automated>
  </verify>
  <acceptance_criteria>
    - 10/10 manual queries return at least 1 relevant chunk in top-3 results
    - Query 1 (Ollama localhost) returns ADR-E02 as a source in top-3
    - Query 3 (Suricata IDS) returns ADR-0007 or suricata-ids-runbook as source
    - Query 9 (N150 throughput) returns benchmark document as source
    - End-to-end LLM test (--llm flag) produces non-empty coherent response referencing corpus content
    - rag_query.py has --llm flag: `grep "llm" scripts/rag_query.py` matches
    - No query returns chunks that are clearly from the wrong document
  </acceptance_criteria>
  <done>All 10 security queries return relevant chunks. End-to-end RAG+LLM test produces coherent answer with corpus context. rag_query.py updated with --llm flag for end-to-end testing.</done>
</task>

<task type="auto">
  <name>Task 2: Verify ChromaDB reboot persistence and run full validation suite</name>
  <files>scripts/validate-phase12.sh</files>
  <read_first>
    scripts/validate-phase12.sh
    .planning/phases/12-rag-knowledge-pipeline/12-VALIDATION.md
  </read_first>
  <action>
**Part A: Reboot persistence test (RAG-01 critical check)**

1. Record pre-reboot collection count:
   ```
   ssh opsadmin@192.168.1.22 "/opt/rag/bin/python3 -c \"import chromadb; c=chromadb.PersistentClient(path='/var/lib/chromadb'); print(c.get_collection('firewall-corpus').count())\""
   ```

2. Reboot supportTAK-server:
   ```
   ssh opsadmin@192.168.1.22 "sudo reboot"
   ```

3. Wait for server to come back online (poll with ssh until connection succeeds, ~60-90 seconds).

4. Verify collection count unchanged:
   ```
   ssh opsadmin@192.168.1.22 "/opt/rag/bin/python3 -c \"import chromadb; c=chromadb.PersistentClient(path='/var/lib/chromadb'); print(c.get_collection('firewall-corpus').count())\""
   ```
   Post-reboot count MUST equal pre-reboot count exactly.

5. Run a query post-reboot to verify retrieval still works:
   ```
   ssh opsadmin@192.168.1.22 "cd ~/Firewall && /opt/rag/bin/python3 scripts/rag_query.py 'SSH hardening'"
   ```

**Part B: Run full validation suite**

Run the complete validation suite including the RAG-04 end-to-end check:
```
bash scripts/validate-phase12.sh --full
```

All checks must PASS. If any FAIL, diagnose and fix before marking complete.

**Part C: Update validate-phase12.sh if needed**

If any issues were found during the 10-query test or reboot test that revealed gaps in the validation script, update the script to cover those cases. Specifically ensure:
- Reboot persistence check compares pre/post counts (can be done as a one-time manual check documented in the validation output)
- The --full flag runs the end-to-end LLM test
- All 4 RAG requirement IDs appear in the validation output
  </action>
  <verify>
    <automated>bash scripts/validate-phase12.sh 2>&1 | tail -5 | grep -q "FAIL: 0"</automated>
  </verify>
  <acceptance_criteria>
    - ChromaDB collection count is identical before and after reboot
    - Post-reboot query returns results (retrieval works after restart)
    - `bash scripts/validate-phase12.sh` passes with 0 FAIL
    - `bash scripts/validate-phase12.sh --full` passes with 0 FAIL (includes RAG-04 end-to-end)
    - Validation output includes checks for RAG-01, RAG-02, RAG-03, RAG-04
  </acceptance_criteria>
  <done>ChromaDB survives reboot with identical chunk count. Full validation suite passes all RAG-01 through RAG-04 checks. Phase 12 success criteria met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify RAG retrieval quality</name>
  <files>scripts/rag_query.py, scripts/validate-phase12.sh</files>
  <action>Human verification checkpoint. Present the user with the verification steps below and wait for approval before marking phase complete.</action>
  <what-built>
    RAG knowledge pipeline with ChromaDB vector store, 150+ indexed chunks from operating corpus (ADRs, runbooks, benchmarks, contracts), query interface with k=3 retrieval, and end-to-end Foundation-Sec-8B integration.
  </what-built>
  <how-to-verify>
    1. Run a query and check the results make sense:
       ```
       ssh opsadmin@192.168.1.22 "cd ~/Firewall && /opt/rag/bin/python3 scripts/rag_query.py 'Why was Suricata set to monitor mode?'"
       ```
       Expected: Top chunks reference ADR-0007 with rationale about false positive tuning before active IPS mode.

    2. Run the validation suite:
       ```
       bash scripts/validate-phase12.sh
       ```
       Expected: All checks PASS, 0 FAIL.

    3. (Optional) If Ollama is loaded and Malcolm paused, test end-to-end:
       ```
       ssh opsadmin@192.168.1.22 "cd ~/Firewall && /opt/rag/bin/python3 scripts/rag_query.py --llm 'Explain the security controls for the AI analyst'"
       ```
       Expected: Coherent response referencing localhost binding, OLLAMA_KEEP_ALIVE, recommendations-only policy.
  </how-to-verify>
  <verify>
    <automated>bash scripts/validate-phase12.sh 2>&1 | tail -5 | grep -q "FAIL: 0"</automated>
  </verify>
  <done>User has verified RAG retrieval quality and approved the pipeline output.</done>
  <resume-signal>Type "approved" or describe any retrieval quality issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `bash scripts/validate-phase12.sh` — all RAG-01, RAG-02, RAG-03 checks PASS
2. `bash scripts/validate-phase12.sh --full` — all checks including RAG-04 end-to-end PASS
3. ChromaDB reboot persistence confirmed (collection count unchanged)
4. 10/10 manual security queries returned relevant technical content
5. Foundation-Sec-8B produced coherent answer with RAG context injected
</verification>

<success_criteria>
- ChromaDB persisted on NVMe, survives reboot (collection count unchanged)
- ADR query returns rationale as top-3 chunk — no chunk splits mid-decision
- 10 manual security queries return relevant technical content
- Foundation-Sec-8B produces coherent answer with RAG context injected — end-to-end confirmed
- validate-phase12.sh --full passes all checks with 0 FAIL
</success_criteria>

<output>
After completion, create `.planning/phases/12-rag-knowledge-pipeline/12-02-SUMMARY.md`
</output>
