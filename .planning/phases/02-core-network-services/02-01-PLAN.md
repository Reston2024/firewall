---
phase: 02-core-network-services
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate-phase2.sh
  - configs/dhcp/dhcpd.conf.local
  - configs/dhcp/fixleases.template
  - configs/dhcp/README.md
autonomous: true
requirements:
  - SVC-01
  - SVC-02
  - SVC-06

must_haves:
  truths:
    - "validate-phase2.sh exists, is syntactically valid bash, and checks SVC-01 through SVC-06"
    - "configs/dhcp/dhcpd.conf.local exists as a ready-to-SCP customization file with the NTP option set"
    - "configs/dhcp/fixleases.template documents the 7-field CSV format with example rows and range comment"
  artifacts:
    - path: "scripts/validate-phase2.sh"
      provides: "Phase 2 integration test runner — checks DHCP, DNS, NTP services on IPFire"
      exports: ["exits 0 on all pass, 1 on any fail"]
      min_lines: 80
    - path: "configs/dhcp/dhcpd.conf.local"
      provides: "DHCP customization file — survives WUI regeneration and Core Updates"
      contains: "ntp-servers"
    - path: "configs/dhcp/fixleases.template"
      provides: "Static lease CSV template in 7-field format"
      contains: "aa:bb:cc:dd:ee"
    - path: "configs/dhcp/README.md"
      provides: "Explains the two-file DHCP consistency model and fixleases workflow"
  key_links:
    - from: "configs/dhcp/dhcpd.conf.local"
      to: "/var/ipfire/dhcp/dhcpd.conf"
      via: "SCP then /usr/sbin/dhcpd -t validates merged config"
      pattern: "dhcpd\\.conf\\.local"
    - from: "scripts/validate-phase2.sh"
      to: "/var/ipfire/dhcp/dhcpd.conf"
      via: "grep check for ntp-servers and dns options"
      pattern: "ntp-servers"
---

<objective>
Create the Phase 2 validation script and DHCP config artifacts (dhcpd.conf.local, fixleases template). These are the scripted and repo-committed artifacts that make the WUI-driven DHCP configuration reproducible.

Purpose: validate-phase2.sh is the Nyquist Wave 0 requirement — every subsequent task needs it for acceptance. The DHCP configs are ready-to-SCP artifacts that the human deploys to IPFire during the Plan 03 checkpoint.

Output:
- scripts/validate-phase2.sh — full Phase 2 test suite (DHCP, DNS, NTP)
- configs/dhcp/dhcpd.conf.local — DHCP customizations that survive WUI regeneration
- configs/dhcp/fixleases.template — static lease CSV template with documented field format
- configs/dhcp/README.md — two-file DHCP consistency model explanation
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-network-services/02-RESEARCH.md
@.planning/phases/02-core-network-services/02-VALIDATION.md

<interfaces>
<!-- Key patterns from Phase 2 research. No exploration needed. -->

DHCP file locations on IPFire:
  /var/ipfire/dhcp/dhcpd.conf        — WUI-generated, OVERWRITTEN on save — do not deploy this
  /var/ipfire/dhcp/dhcpd.conf.local  — Custom options, survives WUI regeneration — deploy this
  /var/ipfire/dhcp/fixleases         — CSV of static leases (7-field) — deploy this

fixleases 7-field CSV format:
  # MAC,IP,hostname,enabled,nextIP,remark,interface
  aa:bb:cc:dd:ee:01,192.168.1.10,server01,on,,,
  aa:bb:cc:dd:ee:02,192.168.1.11,nas01,on,,,
  Rules:
  - Static IPs must be OUTSIDE dynamic range (reserve 192.168.1.2-99 for statics, 100-200 for dynamic)
  - No blank lines or comments in the real fixleases file (template file can have comments)
  - After deploying fixleases, WUI must toggle each entry off/on to generate host blocks in dhcpd.conf

DHCP dynamic range design:
  GREEN: 192.168.1.100-200 (dynamic), 192.168.1.2-99 (reserved for statics)
  Gateway/DNS/NTP: all point to 192.168.1.1 (IPFire GREEN IP)

Validation commands for validate-phase2.sh:
  SVC-01: grep "option ntp-servers 192.168.1.1" /var/ipfire/dhcp/dhcpd.conf
  SVC-02: grep -c "hardware ethernet" /var/ipfire/dhcp/dhcpd.conf (>0 = static leases exist)
  SVC-03: drill -D sigok.verteiltesysteme.net | grep "flags.*ad"
  SVC-04: grep "forward-tls-upstream: yes" /etc/unbound/forward.conf
  SVC-05: ntpq -p | grep '^\*' (star prefix = synced) AND ss -ulnp | grep :123
  SVC-06: ls /etc/rc.d/rc3.d/ | grep -E "S.*dhcp|S.*unbound|S.*ntp"

DHCP syntax check before restart:
  /usr/sbin/dhcpd -t -cf /var/ipfire/dhcp/dhcpd.conf

SysVinit service management on IPFire:
  /etc/init.d/dhcp restart
  /etc/init.d/unbound restart
  /etc/init.d/ntp restart

Phase 1 lesson: SSH is on port 22 (not 222) — confirmed from STATE.md "SSH is on port 22"
Phase 1 lesson: Windows CRLF must be fixed with sed -i 's/\r$//' after SCP
Phase 1 lesson: GREEN IP is 192.168.1.1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create configs/dhcp directory with dhcpd.conf.local and fixleases template</name>
  <files>
    configs/dhcp/dhcpd.conf.local
    configs/dhcp/fixleases.template
    configs/dhcp/README.md
  </files>
  <read_first>
    - C:\Users\ablan\Firewall\.planning\phases\02-core-network-services\02-RESEARCH.md (Pattern 1: DHCP GREEN Zone Configuration, Pattern 2: DHCP Static Leases, Common Pitfalls 2, 3, 6)
    - C:\Users\ablan\Firewall\configs\firewall (check existing config directory structure for naming conventions)
  </read_first>
  <action>
    Create configs/dhcp/ directory (mkdir -p configs/dhcp).

    Create configs/dhcp/dhcpd.conf.local with this exact content:
    ```
    # dhcpd.conf.local — DHCP customizations for GREEN zone
    # Deploy to: /var/ipfire/dhcp/dhcpd.conf.local on IPFire
    # This file is INCLUDED by dhcpd.conf and survives WUI regeneration.
    # Use this file for options the WUI does not expose directly.
    #
    # Phase 2 note: The NTP option is set via the WUI DHCP page (Primary NTP field).
    # This file documents the expected options and adds the DHCP domain-name option
    # which the WUI does not always set explicitly.
    #
    # Verify the WUI has set these in dhcpd.conf after configuration:
    #   option domain-name-servers 192.168.1.1;
    #   option ntp-servers 192.168.1.1;
    #   option routers 192.168.1.1;
    #
    # Deploy and syntax-check:
    #   scp configs/dhcp/dhcpd.conf.local root@192.168.1.1:/var/ipfire/dhcp/
    #   ssh root@192.168.1.1 '/usr/sbin/dhcpd -t -cf /var/ipfire/dhcp/dhcpd.conf'
    #   ssh root@192.168.1.1 '/etc/init.d/dhcp restart'

    # Domain name suffix for LAN clients (optional — remove if not desired)
    # This causes DHCP clients to resolve short names like "server01" as "server01.lan.local"
    # option domain-name "lan.local";

    # Log DHCP activity verbosely (useful during Phase 2 validation)
    # log-facility local7;
    ```

    Create configs/dhcp/fixleases.template with this exact content:
    ```
    # fixleases.template — Static DHCP lease template
    # Deploy as: /var/ipfire/dhcp/fixleases on IPFire (rename from .template)
    #
    # FORMAT: MAC,IP,hostname,enabled,nextIP,remark,interface
    # Field 0: MAC address (lowercase, colon-separated)
    # Field 1: IP address (MUST be outside dynamic range 192.168.1.100-200)
    # Field 2: Hostname (becomes DNS entry automatically — no spaces)
    # Field 3: enabled (use "on" to enable, blank to disable)
    # Field 4: nextIP (leave blank)
    # Field 5: remark (leave blank or short description — no commas)
    # Field 6: interface (leave blank for GREEN default)
    #
    # IP RANGE DESIGN:
    #   Static assignments: 192.168.1.2  - 192.168.1.99   (reserve for known hosts)
    #   Dynamic pool:       192.168.1.100 - 192.168.1.200 (set in DHCP WUI)
    #   IPFire GREEN IP:    192.168.1.1   (gateway — never assign to a client)
    #
    # CRITICAL: After deploying this file, open WUI > Network > DHCP Server.
    # Each entry must be toggled off then on once so WUI writes the host block
    # to dhcpd.conf. Without this step, clients will NOT receive static IPs.
    #
    # DEPLOY STEPS:
    #   1. Edit this file with real MACs and IPs (remove comment lines)
    #   2. scp configs/dhcp/fixleases root@192.168.1.1:/var/ipfire/dhcp/fixleases
    #   3. ssh root@192.168.1.1 'sed -i "s/\r$//" /var/ipfire/dhcp/fixleases'
    #   4. Open WUI > Network > DHCP Server — verify entries appear
    #   5. Toggle each entry off then on to force dhcpd.conf host block generation
    #   6. ssh root@192.168.1.1 '/etc/init.d/dhcp restart'
    #   7. Verify: grep "hardware ethernet" /var/ipfire/dhcp/dhcpd.conf
    #
    # EXAMPLE ENTRIES (replace with real MACs and IPs):
    # aa:bb:cc:dd:ee:01,192.168.1.10,server01,on,,,
    # aa:bb:cc:dd:ee:02,192.168.1.11,nas01,on,,,
    # aa:bb:cc:dd:ee:03,192.168.1.12,workstation01,on,,,
    ```

    Create configs/dhcp/README.md with this exact content:
    ```markdown
    # DHCP Configuration

    IPFire uses two files for DHCP configuration. Both must be consistent or static leases will not work.

    ## File Roles

    | File | Role | Survives WUI Save? | Deploy to IPFire? |
    |------|------|--------------------|-------------------|
    | `/var/ipfire/dhcp/dhcpd.conf` | WUI-generated daemon config | NO (overwritten) | No — WUI generates it |
    | `/var/ipfire/dhcp/dhcpd.conf.local` | Custom options, extended config | YES | Yes — deploy from repo |
    | `/var/ipfire/dhcp/fixleases` | Static lease data store (7-field CSV) | YES | Yes — deploy from repo |

    ## Deployment Order (from Phase 2 runbook)

    1. Configure GREEN DHCP pool via WUI (Network > DHCP Server)
    2. Deploy `fixleases` from this repo (see fixleases.template)
    3. Toggle each static lease in WUI (off then on) to generate `host` blocks in `dhcpd.conf`
    4. Deploy `dhcpd.conf.local` from this repo
    5. Run syntax check: `ssh root@192.168.1.1 '/usr/sbin/dhcpd -t -cf /var/ipfire/dhcp/dhcpd.conf'`
    6. Restart: `ssh root@192.168.1.1 '/etc/init.d/dhcp restart'`

    ## IP Range Design

    | Range | Purpose |
    |-------|---------|
    | 192.168.1.1 | IPFire GREEN (gateway) — do not assign |
    | 192.168.1.2 - 192.168.1.99 | Static lease assignments (edit fixleases) |
    | 192.168.1.100 - 192.168.1.200 | Dynamic DHCP pool (set in WUI) |
    | 192.168.1.201 - 192.168.1.254 | Reserved for future static use |

    ## After Any WUI DHCP Change

    Export the regenerated config back to the repo for reproducibility:
    ```bash
    scp root@192.168.1.1:/var/ipfire/dhcp/dhcpd.conf configs/dhcp/dhcpd.conf
    scp root@192.168.1.1:/var/ipfire/dhcp/fixleases configs/dhcp/fixleases
    ```
    ```

    Run: git add configs/dhcp/ && git commit -m "feat(02-01): add DHCP config templates and README"
  </action>
  <verify>
    From C:\Users\ablan\Firewall:
    bash -c "grep 'ntp-servers' configs/dhcp/dhcpd.conf.local"
    bash -c "grep 'aa:bb:cc:dd:ee' configs/dhcp/fixleases.template"
    bash -c "grep 'forward-tls-upstream\|fixleases' configs/dhcp/README.md || grep 'host block' configs/dhcp/README.md"
    bash -c "git log --oneline -1" must show the DHCP commit.
  </verify>
  <acceptance_criteria>
    - `ls configs/dhcp/` lists dhcpd.conf.local, fixleases.template, and README.md
    - `grep 'ntp-servers' configs/dhcp/dhcpd.conf.local` returns a line (NTP option documented)
    - `grep '192.168.1.100' configs/dhcp/fixleases.template` returns a line (dynamic range boundary documented)
    - `grep 'aa:bb:cc:dd:ee' configs/dhcp/fixleases.template` returns at least one example entry
    - `grep 'hardware ethernet' configs/dhcp/README.md` returns a line explaining host block requirement
    - `grep 'dhcpd -t' configs/dhcp/dhcpd.conf.local` returns the syntax check command
    - `grep 'sed -i.*\\\\r' configs/dhcp/fixleases.template` returns the CRLF fix command
    - `git log --oneline` shows a commit containing "DHCP config templates"
  </acceptance_criteria>
  <done>configs/dhcp/ directory contains dhcpd.conf.local (with NTP option documentation), fixleases.template (with 7-field format and CRLF fix), and README.md (two-file consistency model). All committed to git.</done>
</task>

<task type="auto">
  <name>Task 2: Create validate-phase2.sh covering SVC-01 through SVC-06</name>
  <files>
    scripts/validate-phase2.sh
  </files>
  <read_first>
    - C:\Users\ablan\Firewall\.planning\phases\02-core-network-services\02-RESEARCH.md (Code Examples section: verify DNSSEC, verify DoT, verify NTP, validate DHCP syntax, check auto-start)
    - C:\Users\ablan\Firewall\.planning\phases\02-core-network-services\02-VALIDATION.md (Per-Task Verification Map)
    - C:\Users\ablan\Firewall\scripts\validate-phase1.sh (follow same pass/fail/skip pattern established in Phase 1)
  </read_first>
  <action>
    Create scripts/validate-phase2.sh following the exact same pass()/fail()/skip() pattern as validate-phase1.sh.

    The script runs on IPFire (not the dev machine). It must:
    - Accept no arguments for full run, or --quick for a subset
    - Use REPO_BASE=/root/firewall-repo (consistent with Phase 1)
    - Print === Phase 2 Validation Suite — date === header
    - Print === Results: N pass, N fail === footer
    - Exit 0 if all checks pass, 1 if any fail
    - SKIP (not FAIL) checks that require external test hosts

    Write the script with this exact structure:

    ```bash
    #!/bin/bash
    # validate-phase2.sh — Phase 2 Core Network Services validation
    # Usage: bash /root/firewall-repo/scripts/validate-phase2.sh
    # Run on IPFire appliance (not dev machine)
    # Exits: 0 if all automated checks pass, 1 if any fail

    FAIL=0
    PASS=0

    pass() { echo "PASS: $1"; PASS=$((PASS + 1)); }
    fail() { echo "FAIL: $1"; FAIL=$((FAIL + 1)); }
    skip() { echo "SKIP: $1 (manual verification required)"; }

    echo "=== Phase 2 Validation Suite — $(date) ==="
    echo ""

    # --- SVC-01: DHCP GREEN zone has correct gateway, DNS, NTP options ---
    echo "[SVC-01] DHCP GREEN zone options"
    if grep -q "option routers 192.168.1.1" /var/ipfire/dhcp/dhcpd.conf 2>/dev/null; then
      pass "DHCP option routers = 192.168.1.1"
    else
      fail "DHCP option routers not set to 192.168.1.1 in dhcpd.conf"
    fi

    if grep -q "option domain-name-servers 192.168.1.1" /var/ipfire/dhcp/dhcpd.conf 2>/dev/null; then
      pass "DHCP option domain-name-servers = 192.168.1.1"
    else
      fail "DHCP option domain-name-servers not set to 192.168.1.1 in dhcpd.conf"
    fi

    if grep -q "option ntp-servers 192.168.1.1" /var/ipfire/dhcp/dhcpd.conf 2>/dev/null; then
      pass "DHCP option ntp-servers = 192.168.1.1"
    else
      fail "DHCP option ntp-servers not set to 192.168.1.1 in dhcpd.conf — set Primary NTP in WUI DHCP page"
    fi

    if /etc/init.d/dhcp status 2>/dev/null | grep -qi "running\|started"; then
      pass "dhcpd service is running"
    else
      fail "dhcpd service is NOT running — run: /etc/init.d/dhcp start"
    fi
    echo ""

    # --- SVC-02: Static lease capability (host blocks in dhcpd.conf) ---
    echo "[SVC-02] DHCP static lease capability"
    STATIC_COUNT=$(grep -c "hardware ethernet" /var/ipfire/dhcp/dhcpd.conf 2>/dev/null || echo "0")
    if [ "$STATIC_COUNT" -gt 0 ]; then
      pass "dhcpd.conf contains $STATIC_COUNT static lease host block(s)"
    else
      skip "SVC-02: No static leases configured yet — add entries to fixleases and toggle in WUI"
    fi

    if [ -f /var/ipfire/dhcp/fixleases ]; then
      pass "fixleases file exists at /var/ipfire/dhcp/fixleases"
    else
      skip "SVC-02: fixleases file not deployed — deploy from configs/dhcp/fixleases after editing"
    fi
    echo ""

    # --- SVC-03: DNSSEC validation active (AD flag in response) ---
    echo "[SVC-03] DNSSEC validation"
    if command -v drill >/dev/null 2>&1; then
      DRILL_OUTPUT=$(drill -D sigok.verteiltesysteme.net 2>/dev/null)
      if echo "$DRILL_OUTPUT" | grep -q "flags:.*ad"; then
        pass "DNSSEC AD flag present — sigok.verteiltesysteme.net validates correctly"
      else
        fail "DNSSEC AD flag NOT present — check Unbound DNSSEC config (should be on by default since CU80)"
      fi

      SERVFAIL_OUTPUT=$(drill -D sigfail.verteiltesysteme.net 2>/dev/null)
      if echo "$SERVFAIL_OUTPUT" | grep -qi "SERVFAIL"; then
        pass "DNSSEC enforcement active — sigfail.verteiltesysteme.net returns SERVFAIL as expected"
      else
        fail "DNSSEC not enforcing — sigfail.verteiltesysteme.net should return SERVFAIL"
      fi
    else
      fail "drill command not found — expected on IPFire (part of unbound-utils)"
    fi

    if /etc/init.d/unbound status 2>/dev/null | grep -qi "running\|started"; then
      pass "unbound service is running"
    else
      fail "unbound service is NOT running — run: /etc/init.d/unbound start"
    fi
    echo ""

    # --- SVC-04: DNS-over-TLS to upstream resolvers ---
    echo "[SVC-04] DNS-over-TLS upstream configuration"
    if grep -q "forward-tls-upstream: yes" /etc/unbound/forward.conf 2>/dev/null; then
      pass "forward-tls-upstream: yes found in /etc/unbound/forward.conf"
    else
      fail "forward-tls-upstream NOT enabled — in WUI: Network > DNS Servers > Protocol: TLS"
    fi

    if grep -q "@853#" /etc/unbound/forward.conf 2>/dev/null; then
      pass "TLS hostname entries (@853#hostname) present in forward.conf"
    else
      fail "TLS hostname entries missing from forward.conf — add TLS hostnames in WUI DNS Servers page"
    fi

    if grep -q "1dot1dot1dot1.cloudflare-dns.com\|dns.quad9.net" /etc/unbound/forward.conf 2>/dev/null; then
      pass "Cloudflare or Quad9 TLS hostname found in forward.conf"
    else
      fail "Cloudflare/Quad9 TLS hostnames missing — configure in WUI: Network > DNS Servers"
    fi

    skip "SVC-04 wire verification: tcpdump -i red0 -n port 53 (should show no upstream plaintext DNS)"
    skip "SVC-04 wire verification: tcpdump -i red0 -n port 853 (should show encrypted DoT traffic)"
    echo ""

    # --- SVC-05: NTP synchronized and serving clients ---
    echo "[SVC-05] NTP synchronization and client serving"
    NTP_SYNC=$(ntpq -p 2>/dev/null | grep '^\*')
    if [ -n "$NTP_SYNC" ]; then
      pass "NTP synchronized — active peer: $(echo "$NTP_SYNC" | awk '{print $1, "stratum", $3}')"
    else
      fail "NTP NOT synchronized — check upstream pool reachability: ntpq -p"
    fi

    if ss -ulnp 2>/dev/null | grep -q ':123'; then
      pass "NTP listening on port 123 (serving clients)"
    else
      fail "NTP NOT listening on port 123 — enable 'Provide time to local network' in WUI: Services > Time Server"
    fi

    if /etc/init.d/ntp status 2>/dev/null | grep -qi "running\|started"; then
      pass "ntpd service is running"
    else
      fail "ntpd service is NOT running — run: /etc/init.d/ntp start"
    fi
    echo ""

    # --- SVC-06: All services auto-start at boot ---
    echo "[SVC-06] Service auto-start registration (SysVinit)"
    for svc_pattern in "S.*dhcp" "S.*unbound" "S.*ntp"; do
      if ls /etc/rc.d/rc3.d/ 2>/dev/null | grep -qE "$svc_pattern"; then
        pass "Boot symlink exists: /etc/rc.d/rc3.d/ matches $svc_pattern"
      else
        fail "Boot symlink MISSING for pattern $svc_pattern in /etc/rc.d/rc3.d/"
      fi
    done

    skip "SVC-06 reboot persistence: reboot IPFire and re-run this script to confirm all services start"
    echo ""

    # --- Summary ---
    echo "=== Results: ${PASS} pass, ${FAIL} fail ==="
    if [ "$FAIL" -eq 0 ]; then
      echo "ALL CHECKS PASS"
      exit 0
    else
      echo "PHASE 2 VALIDATION FAILED — resolve failures above"
      exit 1
    fi
    ```

    Make executable and commit:
    chmod +x scripts/validate-phase2.sh
    git add scripts/validate-phase2.sh
    git commit -m "feat(02-01): add validate-phase2.sh covering SVC-01 through SVC-06"
  </action>
  <verify>
    From C:\Users\ablan\Firewall:
    bash -c "bash -n scripts/validate-phase2.sh && echo SYNTAX_OK"
    bash -c "grep 'SVC-01' scripts/validate-phase2.sh"
    bash -c "grep 'SVC-06' scripts/validate-phase2.sh"
    bash -c "grep 'ALL CHECKS PASS' scripts/validate-phase2.sh"
    bash -c "grep 'forward-tls-upstream' scripts/validate-phase2.sh"
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/validate-phase2.sh` exits 0 (no syntax errors)
    - `grep 'SVC-01' scripts/validate-phase2.sh` returns a line
    - `grep 'SVC-02' scripts/validate-phase2.sh` returns a line
    - `grep 'SVC-03' scripts/validate-phase2.sh` returns a line
    - `grep 'SVC-04' scripts/validate-phase2.sh` returns a line
    - `grep 'SVC-05' scripts/validate-phase2.sh` returns a line
    - `grep 'SVC-06' scripts/validate-phase2.sh` returns a line
    - `grep 'forward-tls-upstream' scripts/validate-phase2.sh` returns the DoT check line
    - `grep 'sigok.verteiltesysteme.net' scripts/validate-phase2.sh` returns the DNSSEC test line
    - `grep 'ntpq -p' scripts/validate-phase2.sh` returns the NTP sync check
    - `grep ':123' scripts/validate-phase2.sh` returns the NTP serving check
    - `grep 'ALL CHECKS PASS' scripts/validate-phase2.sh` returns a line
    - `ls -la scripts/validate-phase2.sh` shows executable bit (x) set
    - `git log --oneline` shows a commit containing "validate-phase2.sh"
  </acceptance_criteria>
  <done>scripts/validate-phase2.sh is syntactically valid, checks all six SVC requirements with pass()/fail()/skip() pattern, exits 0/1, committed and executable.</done>
</task>

</tasks>

<verification>
After both tasks complete, run from C:\Users\ablan\Firewall:

```bash
# Syntax check
bash -n scripts/validate-phase2.sh && echo "validate-phase2.sh: OK"

# Verify all SVC IDs covered
for id in SVC-01 SVC-02 SVC-03 SVC-04 SVC-05 SVC-06; do
  grep -q "$id" scripts/validate-phase2.sh && echo "OK: $id" || echo "MISSING: $id"
done

# Verify DHCP artifacts
ls configs/dhcp/
grep 'ntp-servers' configs/dhcp/dhcpd.conf.local
grep 'aa:bb:cc' configs/dhcp/fixleases.template

# Git log
git log --oneline -3
```
</verification>

<success_criteria>
- scripts/validate-phase2.sh: syntactically valid, covers SVC-01 through SVC-06, exits 0/1, executable
- configs/dhcp/dhcpd.conf.local: has NTP option documentation, CRLF-safe deploy instructions
- configs/dhcp/fixleases.template: 7-field format documented, example entries, range design, CRLF fix included
- configs/dhcp/README.md: explains two-file consistency model and WUI toggle requirement
- 2 commits in git history from this plan
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-network-services/02-01-SUMMARY.md` with:
- Files created (validate-phase2.sh, configs/dhcp/*)
- Key patterns documented (fixleases two-file model, NTP warning pitfall)
- What Plan 02 builds on top of (DNS/NTP configs and runbook)
</output>
