---
phase: 08-milestone-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate-all.sh
  - scripts/check-drift.sh
  - docs/telemetry-deployment-runbook.md
autonomous: true
gap_closure: true
requirements: [TEL-06, DASH-03, VAL-11]

must_haves:
  truths:
    - "validate-all.sh sources /opt/telemetry/.env (not /opt/telemetry/telemetry/.env)"
    - "check-drift.sh MANAGED_FILES does not include /var/ipfire/ethernet/settings"
    - "Telemetry runbook Section 2 does not instruct to stop/disable rsyslog"
  artifacts:
    - path: "scripts/validate-all.sh"
      provides: "Unified validation orchestrator with correct .env path"
      contains: "source /opt/telemetry/.env"
    - path: "scripts/check-drift.sh"
      provides: "Drift detection without WUI-managed false positives"
    - path: "docs/telemetry-deployment-runbook.md"
      provides: "Correct deployment instructions"
  key_links:
    - from: "scripts/validate-all.sh"
      to: "/opt/telemetry/.env"
      via: "source command"
      pattern: "source /opt/telemetry/\\.env"
---

<objective>
Fix script wiring defects and documentation errors.

Purpose: Close MEDIUM-severity gap INT-7A (validate-all.sh double-telemetry path), LOW-severity gaps INT-4A (check-drift.sh false-positive drift) and INT-2A (runbook incorrect rsyslog instruction).
Output: Three files corrected in-place with targeted edits.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix validate-all.sh .env path and remove ethernet/settings from check-drift.sh</name>
  <files>scripts/validate-all.sh, scripts/check-drift.sh</files>
  <read_first>
    - scripts/validate-all.sh (full file — find line 88 with double-telemetry path)
    - scripts/check-drift.sh (full file — find /var/ipfire/ethernet/settings in MANAGED_FILES array)
    - .planning/v1.0-MILESTONE-AUDIT.md (INT-7A and INT-4A descriptions)
  </read_first>
  <action>
    Fix 1 — validate-all.sh line 88 (closes INT-7A):
    Change: "source /opt/telemetry/telemetry/.env"
    To:     "source /opt/telemetry/.env"

    This is a single character-level fix on line 88 inside the SSH command string. The double "telemetry/telemetry" path causes the .env source to silently fail, which makes GF_SECURITY_ADMIN_PASSWORD unset, which makes TEL-06 and DASH-03 Grafana API checks SKIP instead of PASS.

    Fix 2 — check-drift.sh MANAGED_FILES array (closes INT-4A):
    Remove the entry "/var/ipfire/ethernet/settings" from the MANAGED_FILES array (currently line 39).
    Also remove the comment on line 38 ("# IPFire ethernet settings (WUI-managed but tracked in integrity baseline)").

    Reason (per D-21): /var/ipfire/ethernet/settings is WUI-managed. Including it in drift detection causes false-positive drift whenever the user changes network settings via WUI.

    Do NOT change any other lines in either file.
  </action>
  <acceptance_criteria>
    - grep -c "telemetry/telemetry" scripts/validate-all.sh returns 0
    - grep -c "source /opt/telemetry/.env" scripts/validate-all.sh returns 1
    - grep -c "ethernet/settings" scripts/check-drift.sh returns 0
    - scripts/validate-all.sh is syntactically valid (bash -n scripts/validate-all.sh exits 0)
    - scripts/check-drift.sh is syntactically valid (bash -n scripts/check-drift.sh exits 0)
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/validate-all.sh && bash -n scripts/check-drift.sh && ! grep -q "telemetry/telemetry" scripts/validate-all.sh && ! grep -q "ethernet/settings" scripts/check-drift.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>validate-all.sh sources correct .env path; check-drift.sh no longer tracks WUI-managed ethernet/settings</done>
</task>

<task type="auto">
  <name>Task 2: Correct telemetry runbook Section 2 rsyslog instruction</name>
  <files>docs/telemetry-deployment-runbook.md</files>
  <read_first>
    - docs/telemetry-deployment-runbook.md (full file — focus on Section 2 and the CRITICAL WARNINGS)
    - .planning/v1.0-MILESTONE-AUDIT.md (INT-2A description)
  </read_first>
  <action>
    Correct Section 2 of docs/telemetry-deployment-runbook.md (closes INT-2A).

    The current Section 2 title is "Check for rsyslog Conflict on supportTAK-server (Pre-Flight)" and Step 2.2 instructs to stop and disable rsyslog. This is INCORRECT per the deployed architecture decision:

    Decision: "Actual Path A architecture is rsyslog->file->Alloy tailing (not direct UDP receive): rsyslog must be running as relay"

    Rewrite Section 2 to:
    1. Change title to: "Verify rsyslog Relay on supportTAK-server (Pre-Flight)"
    2. Change purpose text to explain that rsyslog acts as a relay — it receives UDP syslog from IPFire, writes to a local file, and Alloy tails that file. rsyslog must be RUNNING, not disabled.
    3. Replace Step 2.1 with a check that rsyslog IS running and listening on UDP 514:
       ssh opsadmin@192.168.1.101 'sudo ss -ulnp | grep :514'
       Expected: rsyslog appears bound to UDP 514
    4. Replace Step 2.2 with: "If rsyslog is NOT running — start it"
       ssh opsadmin@192.168.1.101 'sudo systemctl start rsyslog && sudo systemctl enable rsyslog'
    5. Replace Step 2.3 with: "If rsyslog appears — proceed to Section 3"
    6. Update the Note to explain that rsyslog receives IPFire syslog on UDP 514, writes to /var/log/ipfire/ (or configured path), and Alloy tails those files for Loki ingestion.
    7. Update the checkbox to: "rsyslog is running and bound to UDP 514 on supportTAK-server"

    Also fix the CRITICAL WARNINGS section bullet that says "Verify UDP port 514 is NOT held by rsyslog before starting Alloy" — change to: "Verify rsyslog is running and bound to UDP port 514 on supportTAK-server before starting the Alloy container. rsyslog acts as a relay (receives UDP, writes to file); Alloy tails the output file."

    Do NOT change any other sections of the runbook.
  </action>
  <acceptance_criteria>
    - grep -c "stop rsyslog" docs/telemetry-deployment-runbook.md returns 0
    - grep -c "disable rsyslog" docs/telemetry-deployment-runbook.md returns 0
    - grep -c "rsyslog Relay" docs/telemetry-deployment-runbook.md returns >= 1
    - grep -c "systemctl start rsyslog" docs/telemetry-deployment-runbook.md returns >= 1
    - Section 2 no longer instructs to stop or disable rsyslog
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "stop rsyslog" docs/telemetry-deployment-runbook.md && ! grep -q "disable rsyslog" docs/telemetry-deployment-runbook.md && grep -q "start rsyslog" docs/telemetry-deployment-runbook.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Telemetry runbook Section 2 correctly instructs to verify rsyslog is running as relay, not to disable it</done>
</task>

</tasks>

<verification>
- validate-all.sh: no "telemetry/telemetry" string; correct path "source /opt/telemetry/.env" present
- check-drift.sh: no "ethernet/settings" entry; MANAGED_FILES array has 11 entries (was 12)
- telemetry-deployment-runbook.md: no "stop rsyslog" or "disable rsyslog"; Section 2 says "Relay"
- All three files pass bash -n syntax check (for .sh files)
</verification>

<success_criteria>
INT-7A, INT-4A, INT-2A all closed. validate-all.sh TEL-06 and DASH-03 checks will now find the correct .env path. check-drift.sh will not false-positive on WUI-managed files. Runbook matches deployed architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/08-milestone-gap-closure/08-02-SUMMARY.md`
</output>
