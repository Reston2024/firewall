---
phase: 08-milestone-gap-closure
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - manifests/file-manifest.sha256
autonomous: false
gap_closure: true
requirements: [VAL-11, REPO-02]

must_haves:
  truths:
    - "validate-all.sh runs on IPFire with 0 FAIL results"
    - "check-drift.sh --generate produces manifest without ethernet/settings"
    - "deploy-phase4.sh deploys suricata.yaml successfully"
    - "deploy-phase5.sh deploys syslog.conf successfully"
  artifacts:
    - path: "manifests/file-manifest.sha256"
      provides: "Updated file manifest reflecting gap closure changes"
  key_links:
    - from: "scripts/validate-all.sh"
      to: "all validate-phaseN.sh scripts"
      via: "orchestration calls"
      pattern: "validate-phase"
    - from: "scripts/check-drift.sh"
      to: "manifests/file-manifest.sha256"
      via: "manifest generation"
      pattern: "file-manifest.sha256"
---

<objective>
Redeploy fixed files to IPFire, regenerate file manifest, and run full validation suite.

Purpose: Verify all 6 gap closures work end-to-end on live infrastructure. Regenerate the file manifest to reflect the updated configs. Run validate-all.sh to confirm 0 FAIL results.
Output: Updated file-manifest.sha256 committed; validate-all.sh passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08-milestone-gap-closure/08-01-SUMMARY.md
@.planning/phases/08-milestone-gap-closure/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redeploy updated files to IPFire and regenerate manifest</name>
  <files>manifests/file-manifest.sha256</files>
  <read_first>
    - .planning/phases/08-milestone-gap-closure/08-01-SUMMARY.md (what was exported)
    - .planning/phases/08-milestone-gap-closure/08-02-SUMMARY.md (what was fixed)
    - scripts/deploy-phase4.sh (suricata deploy path)
    - scripts/deploy-phase5.sh (syslog deploy path)
    - scripts/check-drift.sh (manifest generation)
    - configs/firewall/backup-include.user (updated backup list)
  </read_first>
  <action>
    Deploy the updated files to IPFire and regenerate the file manifest.

    Step 1 — Deploy updated backup-include.user:
    scp -i ~/.ssh/ipfire_ed25519 configs/firewall/backup-include.user root@192.168.1.1:/var/ipfire/backup/include.user

    Step 2 — Deploy updated validate-all.sh:
    scp -i ~/.ssh/ipfire_ed25519 scripts/validate-all.sh root@192.168.1.1:/root/firewall-repo/scripts/validate-all.sh

    Step 3 — Deploy updated check-drift.sh:
    scp -i ~/.ssh/ipfire_ed25519 scripts/check-drift.sh root@192.168.1.1:/root/firewall-repo/scripts/check-drift.sh

    Step 4 — Regenerate file manifest on IPFire:
    ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'bash /root/firewall-repo/scripts/check-drift.sh --generate'

    Step 5 — Pull regenerated manifest back to repo:
    scp -i ~/.ssh/ipfire_ed25519 root@192.168.1.1:/root/firewall-repo/manifests/file-manifest.sha256 manifests/file-manifest.sha256

    Step 6 — Verify manifest no longer includes ethernet/settings:
    grep "ethernet/settings" manifests/file-manifest.sha256 should return nothing.
  </action>
  <acceptance_criteria>
    - manifests/file-manifest.sha256 exists and is non-empty
    - grep -c "ethernet/settings" manifests/file-manifest.sha256 returns 0
    - grep -c "suricata.yaml" manifests/file-manifest.sha256 returns 1
    - grep -c "syslog.conf" manifests/file-manifest.sha256 returns 1
    - grep -c "forward.conf" configs/firewall/backup-include.user returns 1
  </acceptance_criteria>
  <verify>
    <automated>test -s manifests/file-manifest.sha256 && ! grep -q "ethernet/settings" manifests/file-manifest.sha256 && grep -q "suricata.yaml" manifests/file-manifest.sha256 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Updated scripts and configs deployed to IPFire; manifest regenerated without ethernet/settings; manifest committed to repo</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run validate-all.sh and confirm 0 FAIL results</name>
  <what-built>
    All 6 gap closures from the v1.0 milestone audit:
    - INT-1A: configs/suricata/suricata.yaml exported from live IPFire
    - INT-ORF-1: configs/syslog.conf exported from live IPFire
    - INT-7A: validate-all.sh .env path fixed (no more double-telemetry)
    - INT-6A: /etc/unbound/forward.conf added to backup-include.user
    - INT-4A: /var/ipfire/ethernet/settings removed from check-drift.sh
    - INT-2A: Telemetry runbook Section 2 rsyslog instruction corrected
  </what-built>
  <how-to-verify>
    1. SSH to IPFire and run the full validation suite:
       ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'bash /root/firewall-repo/scripts/validate-all.sh'

    2. Expected: All phases show PASS or SKIP (SKIP is acceptable for off-box Phase 5 if supportTAK-server is unreachable). Zero FAIL results.

    3. Verify TEL-06 and DASH-03 specifically (these were broken by INT-7A):
       ssh opsadmin@192.168.1.101 'source /opt/telemetry/.env && sudo -E bash /opt/telemetry/scripts/validate-phase5.sh'
       Expected: TEL-06 and DASH-03 show PASS (not SKIP).

    4. Run drift check to confirm no drift:
       ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'bash /root/firewall-repo/scripts/check-drift.sh'
       Expected: 0 changed, 0 missing.

    5. Verify backup-include.user is deployed:
       ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'grep forward.conf /var/ipfire/backup/include.user'
       Expected: /etc/unbound/forward.conf appears.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
- validate-all.sh completes with 0 FAIL results
- TEL-06 and DASH-03 show PASS (not SKIP) when run with correct .env
- check-drift.sh shows 0 changed, 0 missing
- File manifest committed to repo matches live system
</verification>

<success_criteria>
All 6 audit gaps (INT-1A, INT-ORF-1, INT-7A, INT-6A, INT-2A, INT-4A) and both flow gaps (FLOW-DR-SURICATA, FLOW-DR-SYSLOG) are closed. validate-all.sh passes. Re-audit would show 0 HIGH/MEDIUM gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/08-milestone-gap-closure/08-03-SUMMARY.md`
</output>
