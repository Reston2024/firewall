---
phase: 04-suricata-ids-ips
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate-phase4.sh
  - scripts/check-suricata-integrity.sh
  - docs/suricata-ids-runbook.md
  - configs/suricata/suricata-yaml-eve-reference.yaml
  - configs/suricata/suricata-yaml-memcap-reference.yaml
autonomous: true
requirements:
  - IDS-01
  - IDS-02
  - IDS-03
  - IDS-04
  - IDS-05
  - IDS-06
  - IDS-07
  - IDS-08

must_haves:
  truths:
    - "validate-phase4.sh exists, is executable, covers IDS-01 through IDS-08 with pass/fail/skip pattern matching prior phases"
    - "check-suricata-integrity.sh exists at scripts/ and contains sha256 baseline comparison plus EVE JSON enabled check"
    - "suricata-ids-runbook.md documents the full ordered deployment procedure: WUI enable, zone selection, ruleset, memcap, EVE verify, sha256 baseline"
    - "EVE JSON reference config block is committed to configs/suricata/ for comparison with live suricata.yaml"
    - "Memcap reference values for N100 16GB single-channel DDR5 are committed to configs/suricata/"
  artifacts:
    - path: "scripts/validate-phase4.sh"
      provides: "Phase 4 validation suite covering IDS-01 through IDS-08"
      contains: "IDS-01"
    - path: "scripts/check-suricata-integrity.sh"
      provides: "Post-Core-Update suricata.yaml integrity check script"
      contains: "sha256sum"
    - path: "docs/suricata-ids-runbook.md"
      provides: "Ordered human deployment procedure for Phase 4 WUI steps"
      contains: "testmynids.org"
    - path: "configs/suricata/suricata-yaml-eve-reference.yaml"
      provides: "Expected EVE JSON config block for comparison with live suricata.yaml"
      contains: "eve-log"
    - path: "configs/suricata/suricata-yaml-memcap-reference.yaml"
      provides: "N100-tuned memcap values for manual entry into suricata.yaml"
      contains: "stream"
  key_links:
    - from: "docs/suricata-ids-runbook.md"
      to: "scripts/validate-phase4.sh"
      via: "Runbook Section 6 instructs human to run validate-phase4.sh"
      pattern: "validate-phase4.sh"
    - from: "scripts/check-suricata-integrity.sh"
      to: "/etc/suricata/suricata.yaml"
      via: "sha256sum -c /root/suricata-yaml.sha256"
      pattern: "sha256sum"
    - from: "configs/suricata/suricata-yaml-memcap-reference.yaml"
      to: "/etc/suricata/suricata.yaml"
      via: "Human applies memcap values via SSH in Runbook Section 4"
      pattern: "stream.memcap"
---

<objective>
Create all pre-deployment artifacts for Phase 4 Suricata IDS/IPS: the validation suite, the post-Core-Update integrity check script, the human deployment runbook, and the Suricata config reference files.

Purpose: Phase 4 follows the proven pattern from Phases 2 and 3 — Claude creates all artifacts in Wave 1 so the human deployment checkpoint (Plan 02) has complete scripts and documentation to follow. No WUI or SSH access is needed for Plan 01.

Output:
- scripts/validate-phase4.sh — covers IDS-01 through IDS-08
- scripts/check-suricata-integrity.sh — post-Core-Update suricata.yaml integrity check
- docs/suricata-ids-runbook.md — ordered WUI + SSH deployment procedure
- configs/suricata/suricata-yaml-eve-reference.yaml — expected EVE JSON block
- configs/suricata/suricata-yaml-memcap-reference.yaml — N100 memcap values
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-suricata-ids-ips/04-RESEARCH.md
@.planning/phases/04-suricata-ids-ips/04-VALIDATION.md
@.planning/phases/03-ssh-hardening-and-management-security/03-01-SUMMARY.md

<interfaces>
<!-- Established patterns from prior phases. Use these exactly — consistency matters. -->

Validation script pattern (from validate-phase3.sh):
  - Functions: pass() { ... }, fail() { ... }, skip() { ... }
  - Counters: PASS=0, FAIL=0, SKIP=0
  - Section headers: echo "--- IDS-01: ..."
  - Requirement ID embedded in each check section
  - Exit: if [ $FAIL -gt 0 ]; then exit 1; else exit 0; fi
  - Final line: "=== ALL CHECKS PASS ===" or "=== CHECKS COMPLETE: N PASS, M FAIL, K SKIP ==="
  - SKIP (not FAIL) for tests that require live traffic, time passage, or cannot be automated

SSH access pattern:
  ssh -i C:\Users\ablan\.ssh\ipfire_ed25519 root@192.168.1.1
  (key auth only — password auth disabled in Phase 3)

CRLF fix (required for all files deployed via SCP from Windows):
  ssh root@192.168.1.1 'sed -i "s/\r$//" /path/to/file'

Repo root: C:\Users\ablan\Firewall
IPFire GREEN IP: 192.168.1.1
Management host: 192.168.1.100

Suricata paths (on IPFire):
  /etc/suricata/suricata.yaml          — Primary config (IPFire template-managed, overwritten by Core Updates)
  /var/lib/suricata/local.rules        — Custom rules (NOT overwritten)
  /var/log/suricata/eve.json           — EVE JSON alert output
  /var/log/suricata/fast.log           — Simple one-line alert log
  /var/log/suricata/suricata.log       — Engine start/stop/errors
  /var/ipfire/suricata/                — IPFire sub-configs (also managed by WUI)
  /root/suricata-yaml.sha256           — sha256 baseline stored here

Test signature:
  curl http://testmynids.org/uid/index.html
  Expected alert: SID 2100498 "GPL ATTACK_RESPONSE id check returned root"
  Verify: tail -5 /var/log/suricata/fast.log | grep "2100498"

Suricata process check:
  pgrep -o suricata    (returns PID if running)
  /etc/init.d/suricata status    (SysVinit — IPFire uses SysVinit, not systemd)

Memory check command:
  cat /proc/$(pgrep -o suricata)/status | grep VmRSS

N100 memcap values (from research — conservative starting point):
  defrag.memcap: 64mb
  flow.memcap: 128mb
  flow.prealloc: 30000
  stream.memcap: 256mb
  stream.reassembly.memcap: 512mb
  stream.reassembly.depth: 1mb
  stream.checksum-validation: no    (required for Intel i226-V NIC offloading)

EVE JSON config block (from IPFire CU200 config diff):
  outputs:
    - eve-log:
        enabled: yes
        filetype: regular
        filename: eve.json
        types:
          - alert: (payload, packet, metadata)
          - http: (extended: yes)
          - dns: (version: 3)
          - tls: (extended: yes)
          - flow

CRITICAL: suricata.yaml is overwritten by Core Updates — NEVER treat manual edits as permanent.
CRITICAL: IPFire uses oinkmaster for rule updates (not suricata-update). Do NOT document suricata-update.
CRITICAL: EVE alerts are NEVER in syslog on IPFire. Only engine start/stop events go to syslog.
CRITICAL: emerging-policy.rules blocks Linux package managers — document as "NEVER enable" in safe baseline.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write validate-phase4.sh and check-suricata-integrity.sh</name>
  <files>
    scripts/validate-phase4.sh
    scripts/check-suricata-integrity.sh
  </files>
  <read_first>
    - C:\Users\ablan\Firewall\scripts\validate-phase3.sh (read in full — replicate exact pass/fail/skip function signatures, counter pattern, exit pattern)
    - C:\Users\ablan\Firewall\.planning\phases\04-suricata-ids-ips\04-RESEARCH.md (Section "Phase Requirements to Test Map" and "Code Examples" — use the documented automated commands exactly)
    - C:\Users\ablan\Firewall\.planning\phases\04-suricata-ids-ips\04-VALIDATION.md (per-task verification map for check notes)
  </read_first>
  <action>
    Create two scripts. Write them to C:\Users\ablan\Firewall\scripts\ using the Write tool.

    SCRIPT 1: scripts/validate-phase4.sh

    Follow validate-phase3.sh structure exactly — same pass()/fail()/skip() functions, same PASS/FAIL/SKIP counters, same section header style. Use Unix line endings (LF only).

    Script header:
    - Shebang: #!/bin/bash
    - Description comment: Phase 4 validation suite — IDS/IPS (Suricata)
    - Run location note: Run ON IPFire via SSH (not from dev machine)
    - Usage: bash /root/firewall-repo/scripts/validate-phase4.sh [--full]
    - FULL flag: if [ "${1}" = "--full" ]; then FULL=1; else FULL=0; fi

    IDS-01 section — ET Community ruleset loaded:
    - Check 1: pgrep -o suricata returns a PID (Suricata process running)
      FAIL if no PID found, with message: "Suricata is not running. Enable IPS via WUI: Firewall > Intrusion Prevention System"
    - Check 2: grep "rules loaded" /var/log/suricata/suricata.log | tail -1 | awk '{print $NF}'
      Extract rule count. FAIL if count < 1000, PASS if >= 1000.
      Skip if suricata.log is empty or rule count line not found — Suricata may not have fully started.

    IDS-02 section — Zone selection (RED + GREEN active):
    - Check: grep -E "red0|green0" /var/log/suricata/suricata.log | grep -i "running\|NFQ\|nfq\|interface" | head -3
      PASS if output is non-empty (interface mention in log confirms zone active)
      SKIP with message: "Zone log evidence not found — verify manually via WUI IPS page that RED and GREEN are selected" if output is empty.
      Note in comment: WUI-configured state cannot be read reliably from CLI; log presence is best available automated check.

    IDS-03 section — Automatic rule update enabled:
    - Check: ls -la /var/lib/suricata/*.rules 2>/dev/null | head -1
      Extract mtime. If newest .rules file is less than 48 hours old (use stat -c %Y or find -newer), PASS.
      SKIP with message: "Rules mtime check: rules may be current or less than 24h since install — verify WUI auto-update toggle is enabled manually" if no .rules files found or cannot determine age.
      Note: fcron.daily runs at 01:25 AM. After initial install, rules were fetched on first WUI Apply.

    IDS-04 section — Monitor-only mode (not Drop):
    - SKIP unconditionally with message: "Monitor mode verification requires WUI — check Firewall > IPS mode toggle shows 'Surveillance' not 'Drop'. Cannot read mode from CLI reliably."
    - This is per the research doc: "Cannot automate from CLI reliably"

    IDS-05 section — Memory within acceptable range (--full only):
    - If FULL=0: SKIP "Memory check requires --full flag and 30 minutes of traffic. Run: bash validate-phase4.sh --full"
    - If FULL=1:
      PID=$(pgrep -o suricata)
      if [ -z "$PID" ]; then skip "Suricata not running — cannot check memory"; continue
      RSS=$(cat /proc/$PID/status 2>/dev/null | grep VmRSS | awk '{print $2}')
      if [ -z "$RSS" ]; then skip "Cannot read VmRSS from /proc/$PID/status"; continue
      RSS_MB=$((RSS / 1024))
      if [ $RSS_MB -gt 2048 ]; then fail "Suricata RSS ${RSS_MB}MB exceeds 2048MB limit. Check /var/log/suricata/eve.json for memcap events and reduce memcap values."
      else pass "Suricata RSS ${RSS_MB}MB is within acceptable range (< 2048MB)"

    IDS-06 section — EVE JSON receiving entries:
    - Check 1: Test file exists and is non-empty
      if [ ! -f /var/log/suricata/eve.json ]; then fail "/var/log/suricata/eve.json does not exist. Check suricata.yaml: grep -A3 'eve-log:' /etc/suricata/suricata.yaml"; fi
    - Check 2: grep -q '"event_type"' /var/log/suricata/eve.json
      FAIL with message: "eve.json exists but contains no event_type entries. Trigger test traffic: curl http://testmynids.org/uid/index.html" if grep returns nothing.
      PASS if entries found.
    - Check 3: grep -A3 "eve-log:" /etc/suricata/suricata.yaml | grep -q "enabled: yes"
      WARN (not FAIL — use skip() with WARN prefix) if enabled: yes not found.
      Message: "WARN: suricata.yaml eve-log section not confirmed enabled. Check: grep -A3 'eve-log:' /etc/suricata/suricata.yaml"
      PASS if enabled: yes confirmed.

    IDS-07 section — No emerging-policy rules in active ruleset:
    - Check: grep "emerging-policy" /var/ipfire/suricata/suricata-used-rulefiles.yaml 2>/dev/null
      PASS if grep returns nothing (emerging-policy not listed = good)
      FAIL with message: "emerging-policy.rules is listed in active rulefiles. This ruleset blocks Linux package managers and can disrupt SSH. Disable via WUI IPS > Customize Ruleset." if grep returns output.
    - Also check: grep -l "emerging-policy" /var/lib/suricata/*.rules 2>/dev/null
      SKIP if /var/ipfire/suricata/suricata-used-rulefiles.yaml does not exist (Suricata not yet configured via WUI).

    IDS-08 section — Integrity check:
    - Check: bash /usr/local/bin/check-suricata-integrity.sh
      PASS if exit 0
      FAIL if exit 1 (no baseline — run: sha256sum /etc/suricata/suricata.yaml > /root/suricata-yaml.sha256)
      SKIP (with message from script) if exit 2 (hash mismatch = WARN from integrity script)
      If /usr/local/bin/check-suricata-integrity.sh does not exist: FAIL "check-suricata-integrity.sh missing from /usr/local/bin/. Deploy from repo: scp + chmod +x"

    Final block (same as validate-phase3.sh):
    - Print totals: PASS=$PASS, FAIL=$FAIL, SKIP=$SKIP
    - if [ $FAIL -gt 0 ]; then echo "=== CHECKS COMPLETE: $PASS PASS, $FAIL FAIL, $SKIP SKIP ==="; exit 1
    - else echo "=== ALL CHECKS PASS ($SKIP skipped) ==="; exit 0; fi

    ---

    SCRIPT 2: scripts/check-suricata-integrity.sh

    This script is also deployed to /usr/local/bin/check-suricata-integrity.sh on IPFire. Write it to scripts/ in the repo (Plan 02 deploys it).

    Use the exact script from RESEARCH.md "Post-Core-Update Integrity Check Script (IDS-08)" section as the base. Extend it with:

    #!/bin/bash
    # Post-Core-Update suricata.yaml integrity check
    # Deploy to: /usr/local/bin/check-suricata-integrity.sh on IPFire
    # Run after every Core Update via: bash /usr/local/bin/check-suricata-integrity.sh
    # Exit codes: 0=OK, 1=FAIL (no baseline or missing file), 2=WARN (hash changed)

    Variables:
      YAML="/etc/suricata/suricata.yaml"
      BASELINE="/root/suricata-yaml.sha256"

    Check 1 — YAML exists:
      if [ ! -f "$YAML" ]; then echo "FAIL: $YAML does not exist"; exit 1; fi

    Check 2 — Baseline present and matches:
      if [ ! -f "$BASELINE" ]; then
        echo "INFO: No baseline found. Creating now."
        sha256sum "$YAML" > "$BASELINE"
        echo "PASS: Baseline created at $BASELINE"
        echo "      Run this script again after any Core Update to detect changes."
        exit 0
      fi
      if sha256sum -c "$BASELINE" --quiet 2>/dev/null; then
        echo "PASS: suricata.yaml matches baseline hash"
      else
        echo "WARN: suricata.yaml has changed since baseline"
        echo "      Core Update may have overwritten custom configuration"
        echo "      Review EVE JSON settings: grep -A5 'eve-log:' $YAML"
        echo "      Review memcap values: grep -E 'memcap|prealloc|depth' $YAML"
        echo "      After restoring config, update baseline:"
        echo "        sha256sum $YAML > $BASELINE"
        exit 2
      fi

    Check 3 — EVE JSON enabled:
      if grep -q "eve-log:" "$YAML"; then
        if grep -A2 "eve-log:" "$YAML" | grep -q "enabled: yes"; then
          echo "PASS: EVE JSON output is enabled"
        else
          echo "WARN: eve-log section found but 'enabled: yes' not confirmed in next 2 lines"
          echo "      Check: grep -A5 'eve-log:' $YAML"
        fi
      else
        echo "WARN: No eve-log section found in $YAML"
        echo "      EVE JSON output may be absent after Core Update"
      fi

    Check 4 — memcap values present (confirms custom tuning survived):
      if grep -q "stream:" "$YAML" && grep -A5 "stream:" "$YAML" | grep -q "memcap:"; then
        STREAM_MEMCAP=$(grep -A5 "stream:" "$YAML" | grep "memcap:" | head -1 | awk '{print $2}')
        echo "PASS: stream.memcap found: $STREAM_MEMCAP"
      else
        echo "WARN: stream.memcap not confirmed in $YAML — may need re-application after Core Update"
      fi

    echo "=== Integrity check complete ==="
    exit 0    (only if no earlier exit was taken)

    Make both scripts executable: they will need chmod +x after SCP deployment (runbook documents this).

    Commit with message: feat(04-01): add validate-phase4.sh and check-suricata-integrity.sh
  </action>
  <verify>
    <automated>
      bash -c "test -f C:/Users/ablan/Firewall/scripts/validate-phase4.sh && echo 'validate-phase4.sh: OK' || echo 'MISSING'"
      bash -c "test -f C:/Users/ablan/Firewall/scripts/check-suricata-integrity.sh && echo 'check-suricata-integrity.sh: OK' || echo 'MISSING'"
      bash -c "grep -c 'IDS-0' C:/Users/ablan/Firewall/scripts/validate-phase4.sh"
      bash -c "grep -q 'sha256sum' C:/Users/ablan/Firewall/scripts/check-suricata-integrity.sh && echo 'sha256sum: OK'"
      bash -c "grep -q 'pass()' C:/Users/ablan/Firewall/scripts/validate-phase4.sh && echo 'pass() function: OK'"
      bash -c "bash -n C:/Users/ablan/Firewall/scripts/validate-phase4.sh && echo 'syntax: OK'"
      bash -c "bash -n C:/Users/ablan/Firewall/scripts/check-suricata-integrity.sh && echo 'syntax: OK'"
    </automated>
  </verify>
  <acceptance_criteria>
    - Both files exist under C:\Users\ablan\Firewall\scripts\
    - validate-phase4.sh contains all 8 section headers matching IDS-01 through IDS-08
    - validate-phase4.sh has pass()/fail()/skip() functions (same as validate-phase3.sh)
    - validate-phase4.sh exits 1 on any FAIL, exits 0 if only PASS and SKIP
    - check-suricata-integrity.sh contains sha256sum and the "WARN: suricata.yaml has changed" message
    - Both scripts pass bash -n syntax check
    - No Windows CRLF line endings (LF only — verified with bash -n or file tool)
    - Committed to git with feat(04-01) commit message
  </acceptance_criteria>
  <done>validate-phase4.sh covers IDS-01 through IDS-08 with automated checks and documented SKIPs for IDS-04 and memory test. check-suricata-integrity.sh detects post-Core-Update suricata.yaml changes. Both scripts committed to scripts/ directory.</done>
</task>

<task type="auto">
  <name>Task 2: Write suricata-ids-runbook.md and Suricata config reference files</name>
  <files>
    docs/suricata-ids-runbook.md
    configs/suricata/suricata-yaml-eve-reference.yaml
    configs/suricata/suricata-yaml-memcap-reference.yaml
  </files>
  <read_first>
    - C:\Users\ablan\Firewall\docs\ssh-management-runbook.md (read in full — replicate structure: numbered sections, critical ordering warnings, sign-off checklist)
    - C:\Users\ablan\Firewall\.planning\phases\04-suricata-ids-ips\04-RESEARCH.md (read fully — "Pattern 1: Monitor-First Deployment", "Pattern 2: Incremental Rule Category Expansion", "Pattern 3: EVE JSON Integrity Baseline", "Common Pitfalls", "Code Examples", "Memcap Tuning" sections)
  </read_first>
  <action>
    Create three files. Use the Write tool. Use Unix line endings (LF only).

    FILE 1: docs/suricata-ids-runbook.md

    Mirror ssh-management-runbook.md structure exactly: numbered sections, prerequisite blocks, verification commands after each step, sign-off checklist at end.

    HEADER:
    # Suricata IDS/IPS Deployment Runbook — Phase 4
    (date, IPFire version CU200, Suricata version 8.0.3)

    CRITICAL WARNINGS block at top:
    - Suricata is managed via WUI — do NOT edit suricata-used-rulefiles.yaml directly
    - emerging-policy.rules MUST NOT be enabled in initial deployment — blocks package managers
    - ALWAYS enable Surveillance (monitor) mode first — NEVER Drop mode on day one
    - suricata.yaml is overwritten by Core Updates — sha256 baseline is mandatory after every manual yaml edit
    - EVE alerts are NOT in syslog — /var/log/suricata/eve.json is the only alert source

    SECTION 1: Prerequisites
    - Phase 3 completed (SSH key auth working)
    - SSH access: ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1
    - WUI access: https://192.168.1.1:444
    - Dev machine: C:\Users\ablan\Firewall repo cloned and current

    SECTION 2: Enable Suricata via WUI (IDS-01, IDS-02, IDS-04)
    - Navigate to: Firewall > Intrusion Prevention System
    - Step 2.1: Check "Enable IPS" checkbox
    - Step 2.2: Zone selection — check RED and GREEN (do NOT check BLUE or ORANGE unless those zones are actively used)
      Note: IPFire lead dev recommends enabling all active zones; no double-processing penalty
    - Step 2.3: Mode — select "Surveillance" (monitor-only, NOT "Drop")
    - Step 2.4: In Rulesets section, enable "Emerging Threats Community" checkbox
    - Step 2.5: Enable "Automatic Updates" toggle for ET Community
    - Step 2.6: IMPORTANT — do NOT enable IPFire DBL (beta in CU200, unstable)
    - Step 2.7: IMPORTANT — do NOT enable emerging-policy category
    - Step 2.8: Click Apply button — Suricata will start, rules will download (~20,742 rules)
    Verify (SSH):
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'pgrep suricata && echo RUNNING'
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'grep "rules loaded" /var/log/suricata/suricata.log | tail -1'
      Expected: 10000+ rules loaded (ET Community default-enabled subset ~20k total)
    Note: flowbit warnings in suricata.log are EXPECTED and harmless — not errors.

    SECTION 3: Verify EVE JSON output (IDS-06)
    - Step 3.1: Check file exists:
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'ls -la /var/log/suricata/eve.json'
    - Step 3.2: Verify EVE JSON enabled in suricata.yaml:
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'grep -A3 "eve-log:" /etc/suricata/suricata.yaml'
      Expected: enabled: yes
      If not: proceed to Step 3.3. If yes: skip to Section 4.
    - Step 3.3 (if EVE JSON disabled — edit suricata.yaml):
      Compare C:\Users\ablan\Firewall\configs\suricata\suricata-yaml-eve-reference.yaml with live:
        ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'grep -A20 "^outputs:" /etc/suricata/suricata.yaml'
      If eve-log block is missing or disabled: edit /etc/suricata/suricata.yaml to set enabled: yes
      CRITICAL: After any manual yaml edit, immediately update baseline (see Section 5).
    - Step 3.4: Test signature trigger:
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'curl -s http://testmynids.org/uid/index.html'
      Expected response contains: "uid=0(root) gid=0(root) groups=0(root)"
    - Step 3.5: Verify alert appeared in both logs:
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'tail -5 /var/log/suricata/fast.log'
      Expected: [1:2100498:7] GPL ATTACK_RESPONSE id check returned root
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'tail -10 /var/log/suricata/eve.json | grep event_type'
      Expected: "alert" entries in output

    SECTION 4: Apply N100 memcap values (IDS-05)
    - Context: Default suricata.yaml has conservative defaults. Apply tuned values for 16GB single-channel DDR5.
    - Reference file: C:\Users\ablan\Firewall\configs\suricata\suricata-yaml-memcap-reference.yaml
    - Step 4.1: Back up suricata.yaml before editing:
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'cp /etc/suricata/suricata.yaml /etc/suricata/suricata.yaml.bak.$(date +%Y%m%d)'
    - Step 4.2: Apply memcap values via SSH heredoc or sed:
      Show exact sed commands for each memcap value (derived from configs/suricata/suricata-yaml-memcap-reference.yaml):
        a) stream.memcap: 256mb
        b) stream.reassembly.memcap: 512mb
        c) stream.reassembly.depth: 1mb
        d) stream.checksum-validation: no   (REQUIRED for Intel i226-V NIC hardware checksum offload)
        e) flow.memcap: 128mb
        f) flow.prealloc: 30000
        g) defrag.memcap: 64mb
      Provide the sed command for each:
        ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 "sed -i 's/memcap: 64mb/memcap: 256mb/' /etc/suricata/suricata.yaml"
        Note: sed approach is fragile if default values differ — provide a note that human should verify each change with grep after applying.
    - Step 4.3: Verify values applied:
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'grep -E "memcap:|prealloc:|depth:|checksum" /etc/suricata/suricata.yaml'
    - Step 4.4: Restart Suricata to apply:
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 '/etc/init.d/suricata restart'
      Verify still running: ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'pgrep suricata && echo RUNNING'
    - CRITICAL: After applying memcap values, proceed immediately to Section 5 (sha256 baseline) before any Core Update.

    SECTION 5: Capture sha256 baseline (IDS-08)
    - Context: Core Updates overwrite /etc/suricata/suricata.yaml. The baseline detects this.
    - Step 5.1: Deploy check-suricata-integrity.sh to IPFire:
      scp C:\Users\ablan\Firewall\scripts\check-suricata-integrity.sh root@192.168.1.1:/usr/local/bin/check-suricata-integrity.sh
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'sed -i "s/\r$//" /usr/local/bin/check-suricata-integrity.sh && chmod +x /usr/local/bin/check-suricata-integrity.sh'
    - Step 5.2: Create initial baseline (also creates it if missing):
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'sha256sum /etc/suricata/suricata.yaml > /root/suricata-yaml.sha256 && cat /root/suricata-yaml.sha256'
    - Step 5.3: Verify integrity script works:
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'bash /usr/local/bin/check-suricata-integrity.sh'
      Expected: PASS on all checks
    - After every Core Update: run bash /usr/local/bin/check-suricata-integrity.sh and re-apply memcap values if WARN appears.
    - Add /root/suricata-yaml.sha256 to backup include list:
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'grep -q suricata-yaml.sha256 /var/ipfire/backup/include.user || echo "/root/suricata-yaml.sha256" >> /var/ipfire/backup/include.user'

    SECTION 6: Run validation suite (all IDS-0x)
    - Step 6.1: Deploy scripts to IPFire:
      scp C:\Users\ablan\Firewall\scripts\validate-phase4.sh root@192.168.1.1:/root/firewall-repo/scripts/validate-phase4.sh
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'sed -i "s/\r$//" /root/firewall-repo/scripts/validate-phase4.sh && chmod +x /root/firewall-repo/scripts/validate-phase4.sh'
    - Step 6.2: Run quick suite:
      ssh -i ~/.ssh/ipfire_ed25519 root@192.168.1.1 'bash /root/firewall-repo/scripts/validate-phase4.sh'
      Expected: ALL CHECKS PASS (IDS-04 and IDS-03 may be SKIP — both are acceptable)
    - Step 6.3: If any FAIL, see troubleshooting table in runbook (include table with FAIL message -> fix action)

    SECTION 7: Export live configs to git (post-deployment)
    - Export suricata.yaml from IPFire:
      scp root@192.168.1.1:/etc/suricata/suricata.yaml C:\Users\ablan\Firewall\configs\suricata\suricata.yaml
    - Export backup include.user:
      scp root@192.168.1.1:/var/ipfire/backup/include.user C:\Users\ablan\Firewall\configs\suricata\backup-include.user
    - Commit:
      git add configs/suricata/
      git commit -m "chore(04-02): export live Suricata configs after Phase 4 deployment"

    SIGN-OFF CHECKLIST:
    Map each IDS-0x to its verification status (same format as ssh-management-runbook.md sign-off):
    - [ ] IDS-01: ET Community rules loaded, rule count > 1000 (verify: grep "rules loaded" suricata.log)
    - [ ] IDS-02: RED + GREEN zones selected in WUI IPS page
    - [ ] IDS-03: Auto-update enabled in WUI ET Community toggle
    - [ ] IDS-04: Surveillance (monitor) mode active — NOT Drop
    - [ ] IDS-05: N100 memcap values applied and Suricata restarted
    - [ ] IDS-06: EVE JSON at /var/log/suricata/eve.json receiving entries (curl testmynids.org verified)
    - [ ] IDS-07: emerging-policy.rules NOT in active ruleset
    - [ ] IDS-08: sha256 baseline created, check-suricata-integrity.sh returning PASS

    ---

    FILE 2: configs/suricata/suricata-yaml-eve-reference.yaml

    HEADER comment:
    # EVE JSON configuration reference block
    # REFERENCE ONLY — not deployed directly
    # Compare with live: grep -A30 "^outputs:" /etc/suricata/suricata.yaml
    # Source: IPFire CU200 suricata.yaml template (nopaste.ipfire.org/view/mKfhrhSu)
    # If EVE JSON is disabled after Core Update, this block shows expected configuration

    Write the full EVE JSON config block from the research doc exactly:
    outputs:
      - eve-log:
          enabled: yes
          filetype: regular
          filename: eve.json
          types:
            - alert:
                payload: yes
                payload-buffer-size: 4kb
                payload-printable: yes
                packet: yes
                metadata: yes
            - http:
                extended: yes
            - dns:
                version: 3
            - tls:
                extended: yes
            - flow

    ---

    FILE 3: configs/suricata/suricata-yaml-memcap-reference.yaml

    HEADER comment:
    # Suricata memcap tuning reference — N100 16GB single-channel DDR5
    # REFERENCE ONLY — not deployed directly
    # Apply values manually to /etc/suricata/suricata.yaml via SSH (see suricata-ids-runbook.md Section 4)
    # CRITICAL: After applying, run: sha256sum /etc/suricata/suricata.yaml > /root/suricata-yaml.sha256
    # CRITICAL: Core Updates overwrite suricata.yaml — re-apply after each Core Update if check-suricata-integrity.sh warns
    # Hardware: Intel N100, 16GB single-channel DDR5 ~50GB/s bandwidth
    # Estimated steady-state: 400-600MB RSS; maximum allocation: ~960MB if all pools fill

    Write all memcap values as documented in research doc:
    defrag:
      memcap: 64mb
      hash-size: 65536
      trackers: 65535
      max-frags: 65535

    flow:
      memcap: 128mb
      hash-size: 65536
      prealloc: 30000

    stream:
      memcap: 256mb
      checksum-validation: no
      reassembly:
        memcap: 512mb
        depth: 1mb

    Add comment block explaining checksum-validation: no:
    # checksum-validation: no is REQUIRED for Intel i226-V NIC hardware
    # i226-V performs checksum calculation in hardware; Suricata sees incomplete checksums
    # as invalid before the NIC finalizes them. This causes flooding of ICMP invalid checksum
    # alerts. Setting no suppresses false positive checksum alerts on i226-V hardware.

    Add memory verification commands as a comment block at the bottom:
    # Verification after 30 minutes of normal traffic:
    # cat /proc/$(pgrep -o suricata)/status | grep VmRSS
    # tail -100 /var/log/suricata/eve.json | jq 'select(.event_type=="stats") | .stats.flow.memuse'
    # grep -i "emergency" /var/log/suricata/suricata.log  (should return nothing)

    ---

    After creating all three files, create the configs/suricata/ directory first (mkdir) if needed, then write files. Commit with message:
    feat(04-01): add suricata-ids-runbook.md and suricata config references
  </action>
  <verify>
    <automated>
      bash -c "test -f C:/Users/ablan/Firewall/docs/suricata-ids-runbook.md && echo 'runbook: OK' || echo 'MISSING'"
      bash -c "test -f C:/Users/ablan/Firewall/configs/suricata/suricata-yaml-eve-reference.yaml && echo 'eve-ref: OK' || echo 'MISSING'"
      bash -c "test -f C:/Users/ablan/Firewall/configs/suricata/suricata-yaml-memcap-reference.yaml && echo 'memcap-ref: OK' || echo 'MISSING'"
      bash -c "grep -c 'IDS-0' C:/Users/ablan/Firewall/docs/suricata-ids-runbook.md"
      bash -c "grep -q 'testmynids.org' C:/Users/ablan/Firewall/docs/suricata-ids-runbook.md && echo 'test-sig: OK'"
      bash -c "grep -q 'eve-log' C:/Users/ablan/Firewall/configs/suricata/suricata-yaml-eve-reference.yaml && echo 'eve-log: OK'"
      bash -c "grep -q 'stream' C:/Users/ablan/Firewall/configs/suricata/suricata-yaml-memcap-reference.yaml && echo 'stream: OK'"
      bash -c "grep -q 'checksum-validation' C:/Users/ablan/Firewall/configs/suricata/suricata-yaml-memcap-reference.yaml && echo 'checksum: OK'"
    </automated>
  </verify>
  <acceptance_criteria>
    - suricata-ids-runbook.md exists with all 7 sections (Prerequisites, Enable via WUI, EVE JSON, Memcap, sha256 baseline, Run validation, Export configs)
    - suricata-ids-runbook.md contains testmynids.org test signature verification commands
    - suricata-ids-runbook.md sign-off checklist covers IDS-01 through IDS-08
    - suricata-yaml-eve-reference.yaml contains the full EVE JSON config block with enabled: yes
    - suricata-yaml-memcap-reference.yaml contains all N100 memcap values including stream, flow, and defrag
    - suricata-yaml-memcap-reference.yaml contains checksum-validation: no with explanation of i226-V hardware offload
    - All 3 files committed to git under feat(04-01)
  </acceptance_criteria>
  <done>suricata-ids-runbook.md is the complete ordered procedure for human deployment. Both config reference files document the exact values for EVE JSON and memcap. Plan 02 human checkpoint can use these artifacts without any additional research.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# All 5 Plan 01 files exist
for f in \
  scripts/validate-phase4.sh \
  scripts/check-suricata-integrity.sh \
  docs/suricata-ids-runbook.md \
  configs/suricata/suricata-yaml-eve-reference.yaml \
  configs/suricata/suricata-yaml-memcap-reference.yaml; do
  test -f "C:/Users/ablan/Firewall/$f" && echo "OK: $f" || echo "MISSING: $f"
done

# validate-phase4.sh covers all 8 IDS requirements
for id in IDS-01 IDS-02 IDS-03 IDS-04 IDS-05 IDS-06 IDS-07 IDS-08; do
  grep -q "$id" C:/Users/ablan/Firewall/scripts/validate-phase4.sh && echo "OK: $id" || echo "MISSING: $id"
done

# Scripts pass syntax check
bash -n C:/Users/ablan/Firewall/scripts/validate-phase4.sh && echo "validate-phase4.sh syntax: OK"
bash -n C:/Users/ablan/Firewall/scripts/check-suricata-integrity.sh && echo "check-suricata-integrity.sh syntax: OK"

# Git commits present
cd C:/Users/ablan/Firewall && git log --oneline -5 | grep "04-01"
```
</verification>

<success_criteria>
- scripts/validate-phase4.sh: exists, covers IDS-01 through IDS-08, passes bash -n syntax check
- scripts/check-suricata-integrity.sh: exists, contains sha256sum comparison and EVE JSON enabled check
- docs/suricata-ids-runbook.md: exists with 7 sections, testmynids.org test, IDS-01 through IDS-08 sign-off checklist
- configs/suricata/suricata-yaml-eve-reference.yaml: exists with enabled: yes EVE JSON block
- configs/suricata/suricata-yaml-memcap-reference.yaml: exists with N100 memcap values and checksum-validation: no
- All files committed to git under feat(04-01) commit messages
- No files contain Windows CRLF line endings
</success_criteria>

<output>
After completion, create `.planning/phases/04-suricata-ids-ips/04-01-SUMMARY.md` covering:
- All 5 files created and their key content
- validate-phase4.sh check coverage (which IDS IDs automated vs SKIP)
- Decisions made (e.g., IDS-04 always SKIP, IDS-05 only in --full mode)
- Any deviations from plan
- What Plan 02 uses from this plan
</output>
