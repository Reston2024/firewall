---
phase: 05-telemetry-pipeline-and-dashboards
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - docs/telemetry-deployment-runbook.md
autonomous: false
requirements:
  - TEL-01
  - TEL-02
  - TEL-03
  - TEL-04
  - TEL-05
  - TEL-06
  - TEL-07
  - TEL-08

must_haves:
  truths:
    - "telemetry-deployment-runbook.md is committed to repo with complete ordered procedure for stack deployment and syslog path verification"
    - "Docker Compose stack is running on supportTAK-server with all 5 containers in running state"
    - "IPFire syslog forwarding is configured to 192.168.1.101 UDP 514"
    - "Alloy is receiving syslog from IPFire and entries are visible in Loki within 60 seconds of network activity"
    - "validate-phase5.sh TEL-01 through TEL-08 pass (TEL-07 SKIP is acceptable)"
  artifacts:
    - path: "docs/telemetry-deployment-runbook.md"
      provides: "Ordered human deployment and configuration procedure for Phase 5"
      contains: "192.168.1.101"
    - path: "/opt/telemetry/ on supportTAK-server"
      provides: "Live telemetry stack accepting syslog from IPFire"
      contains: "docker-compose.yml"
  key_links:
    - from: "docs/telemetry-deployment-runbook.md"
      to: "scripts/validate-phase5.sh"
      via: "Runbook Section 5 instructs human to run validate-phase5.sh"
      pattern: "validate-phase5.sh"
    - from: "IPFire WUI Logs > Log Settings"
      to: "Alloy container port 514/udp"
      via: "IPFire syslog UDP forward to 192.168.1.101:514"
      pattern: "192.168.1.101"
---

<objective>
Create the telemetry deployment runbook and execute the human deployment checkpoint: deploy the Docker Compose stack to supportTAK-server, configure IPFire syslog forwarding, and verify the syslog path (Path A) is live — firewall drops appearing in Loki within 60 seconds.

Purpose: This is the Wave 2 human checkpoint that validates all Wave 1 repo artifacts work on the live host. Following the Phase 4 pattern, the human deploys the pre-built stack using the runbook, then runs validate-phase5.sh to confirm the syslog path is operational before proceeding to the EVE JSON path (Plan 03).

Output:
- docs/telemetry-deployment-runbook.md — complete ordered procedure (Wave 1 task, created by Claude before checkpoint)
- Live Docker Compose stack on 192.168.1.101 with all 5 containers running (Wave 2 checkpoint)
- IPFire syslog forwarding active to 192.168.1.101:514 (Wave 2 checkpoint)
- Syslog entries appearing in Loki (Wave 2 checkpoint)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-telemetry-pipeline-and-dashboards/05-RESEARCH.md
@.planning/phases/05-telemetry-pipeline-and-dashboards/05-01-SUMMARY.md

<interfaces>
<!-- Key constants for this plan. Use exactly. -->

Telemetry host: supportTAK-server
  IP: 192.168.1.101
  User: opsadmin
  SSH from dev machine: ssh opsadmin@192.168.1.101
  Stack root: /opt/telemetry/

IPFire:
  IP: 192.168.1.1
  SSH from dev machine: ssh -i C:\Users\ablan\.ssh\ipfire_ed25519 root@192.168.1.1
  SSH from supportTAK-server: ssh -i /home/opsadmin/.ssh/ipfire_ed25519 root@192.168.1.1

Repo root (dev machine): C:\Users\ablan\Firewall
Telemetry subdir: C:\Users\ablan\Firewall\telemetry\
Validation script: C:\Users\ablan\Firewall\scripts\validate-phase5.sh

SCP from dev machine to supportTAK-server:
  scp -r C:/Users/ablan/Firewall/telemetry/ opsadmin@192.168.1.101:/opt/

CRLF fix (required for all bash scripts SCP'd from Windows to Linux):
  ssh opsadmin@192.168.1.101 'find /opt/telemetry/scripts/ -name "*.sh" -exec sed -i "s/\r$//" {} \;'
  ssh opsadmin@192.168.1.101 'find /opt/telemetry/ -name "validate-phase5.sh" -exec sed -i "s/\r$//" {} \;'

Docker Compose v2 command (NOT v1 docker-compose):
  docker compose (with space) — v2 plugin syntax

Grafana URL: http://192.168.1.101:3000
Alloy debug UI: http://192.168.1.101:12345
Loki API: http://192.168.1.101:3100

IPFire syslog WUI path: Logs > Log Settings
  Field: "Syslog server" → 192.168.1.101
  Protocol: UDP (only option)
  Port: 514 (hardcoded, cannot change)

Test FORWARDFW log line verification:
  From dev machine: logger -n 192.168.1.101 -P 514 --udp "testmsg FORWARDFW DROP SRC=203.0.113.42 DST=192.168.1.100 PROTO=TCP DPT=22"
  Verify in Loki:
    curl -s -G 'http://192.168.1.101:3100/loki/api/v1/query_range' \
      --data-urlencode 'query={job="ipfire-syslog"} |= "FORWARDFW"' \
      --data-urlencode 'limit=5' | jq '.data.result[0].values[-1][1]'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write telemetry-deployment-runbook.md</name>
  <files>
    docs/telemetry-deployment-runbook.md
  </files>
  <read_first>
    - C:\Users\ablan\Firewall\docs\suricata-ids-runbook.md (read in full — replicate structure: numbered sections, prerequisite blocks, critical warnings, verification commands after each step, sign-off checklist)
    - C:\Users\ablan\Firewall\.planning\phases\05-telemetry-pipeline-and-dashboards\05-RESEARCH.md (Pattern 5: EVE rsync setup, Pattern 6: IPFire syslog config, Pattern 7: Grafana provisioning, Common Pitfalls, Code Examples "Verify Stack Health After Deploy")
    - C:\Users\ablan\Firewall\.planning\phases\05-telemetry-pipeline-and-dashboards\05-01-SUMMARY.md (what was created in Plan 01 — file paths and config decisions)
  </read_first>
  <action>
    Create docs/telemetry-deployment-runbook.md. Use the Write tool. Use LF line endings only.

    Mirror suricata-ids-runbook.md structure: numbered sections, prerequisite blocks, verification commands after every step, critical warnings at top, sign-off checklist at end.

    HEADER:
    # Telemetry Pipeline Deployment Runbook — Phase 5
    (date, stack versions: Loki 3.6.0, Alloy 1.14.1, Grafana 12.4.1, Prometheus 3.10.0)

    CRITICAL WARNINGS block at top:
    - Deploy Docker Compose stack BEFORE configuring IPFire syslog forwarding (stack must be listening on 514 before logs arrive)
    - Verify UDP port 514 is NOT held by rsyslog before starting Alloy (pitfall 4 from research)
    - NEVER use docker-compose (v1) — use "docker compose" (v2 plugin) on supportTAK-server
    - Grafana admin password "changeme" is default — change on first login; update validate-phase5.sh TEL-06 check if changed
    - EVE JSON path (rsync) is configured in Plan 03 — this runbook covers syslog path only
    - IPFire syslog uses RFC3164 (no year in timestamp) — Alloy handles this via rfc3164_default_to_current_year=true in config.alloy

    SECTION 1: Prerequisites
    - Phase 4 completed: Suricata running, /var/log/suricata/eve.json confirmed active
    - SSH to supportTAK-server: ssh opsadmin@192.168.1.101
    - SSH to IPFire: ssh -i C:\Users\ablan\.ssh\ipfire_ed25519 root@192.168.1.1
    - WUI access: https://192.168.1.1:444
    - Dev machine: C:\Users\ablan\Firewall repo at current commit (Plan 01 complete)
    - supportTAK-server: Docker installed (verify: ssh opsadmin@192.168.1.101 'docker --version && docker compose version')

    SECTION 2: Check for rsyslog conflict on supportTAK-server (pre-flight)
    - Step 2.1: Check if rsyslog is binding UDP 514:
        ssh opsadmin@192.168.1.101 'sudo ss -ulnp | grep :514'
    - Step 2.2: If rsyslog appears:
        ssh opsadmin@192.168.1.101 'sudo systemctl stop rsyslog && sudo systemctl disable rsyslog'
        Verify: ssh opsadmin@192.168.1.101 'sudo ss -ulnp | grep :514'
        Expected: no output (port is free)
    - Step 2.3: If nothing appears: port is free, proceed to Section 3.
    Note: rsyslog.service is optional on monitoring hosts — Alloy will be the syslog receiver.

    SECTION 3: Deploy Docker Compose stack to supportTAK-server
    - Step 3.1: Create stack root directory:
        ssh opsadmin@192.168.1.101 'sudo mkdir -p /opt/telemetry && sudo chown opsadmin:opsadmin /opt/telemetry'
    - Step 3.2: SCP telemetry files from dev machine:
        scp -r C:/Users/ablan/Firewall/telemetry/ opsadmin@192.168.1.101:/opt/
        Note: This copies all 7 config files including alloy/config.alloy, loki/loki-config.yml, etc.
    - Step 3.3: Fix CRLF on all scripts (required — files SCP'd from Windows):
        ssh opsadmin@192.168.1.101 'find /opt/telemetry -name "*.sh" -exec sed -i "s/\r$//" {} \; && echo "CRLF fix: OK"'
    - Step 3.4: Create EVE JSON staging directory:
        ssh opsadmin@192.168.1.101 'sudo mkdir -p /var/log/ipfire-eve && sudo chown opsadmin:opsadmin /var/log/ipfire-eve'
    - Step 3.5: Pull Docker images (may take 5-10 minutes on first pull):
        ssh opsadmin@192.168.1.101 'docker compose -f /opt/telemetry/docker-compose.yml pull'
    - Step 3.6: Start the stack:
        ssh opsadmin@192.168.1.101 'docker compose -f /opt/telemetry/docker-compose.yml up -d'
    - Step 3.7: Verify all 5 containers are running:
        ssh opsadmin@192.168.1.101 'docker compose -f /opt/telemetry/docker-compose.yml ps'
        Expected: loki, alloy, prometheus, node-exporter, grafana — all "running"
        If any container exits immediately, check logs: docker compose -f /opt/telemetry/docker-compose.yml logs [service-name] --tail=50
    - Step 3.8: Verify Loki is ready:
        curl -s http://192.168.1.101:3100/ready
        Expected: "ready"
    - Step 3.9: Verify Grafana is accessible:
        Open http://192.168.1.101:3000 in browser
        Login: admin / changeme
        Change password when prompted (record new password — needed for DASH-03 verification)
    - Step 3.10: Verify Alloy is binding UDP 514:
        ssh opsadmin@192.168.1.101 'sudo ss -ulnp | grep 514'
        Expected: docker-proxy or process on port 514
        Open Alloy debug UI: http://192.168.1.101:12345 — check components show no errors

    SECTION 4: Deploy validate-phase5.sh to supportTAK-server
    - Step 4.1: SCP validate-phase5.sh:
        scp C:/Users/ablan/Firewall/scripts/validate-phase5.sh opsadmin@192.168.1.101:/opt/telemetry/scripts/validate-phase5.sh
    - Step 4.2: Fix CRLF and set executable:
        ssh opsadmin@192.168.1.101 'sed -i "s/\r$//" /opt/telemetry/scripts/validate-phase5.sh && chmod +x /opt/telemetry/scripts/validate-phase5.sh'
    - Step 4.3: Verify syntax:
        ssh opsadmin@192.168.1.101 'bash -n /opt/telemetry/scripts/validate-phase5.sh && echo syntax OK'

    SECTION 5: Configure IPFire syslog forwarding (TEL-01)
    - Context: IPFire WUI writes /etc/syslog.conf. The WUI setting is the only persistent way to configure syslog forwarding.
    - Step 5.1: Navigate to: https://192.168.1.1:444 > Logs > Log Settings
    - Step 5.2: In "Remote Logging" section:
        Syslog server field: enter 192.168.1.101
        Protocol: UDP (only option)
        Click Save
    - Step 5.3: Verify syslog.conf was updated:
        ssh -i C:/Users/ablan/.ssh/ipfire_ed25519 root@192.168.1.1 'grep "192.168.1.101" /etc/syslog.conf'
        Expected: a line containing 192.168.1.101 and @192.168.1.101 (UDP forwarding notation)
    - Step 5.4: Restart IPFire syslog daemon to apply:
        ssh -i C:/Users/ablan/.ssh/ipfire_ed25519 root@192.168.1.1 '/etc/init.d/syslog restart'
    Note: Port 514 is hardcoded in IPFire. The WUI does not expose port selection.
    Note: TCP syslog is not supported in IPFire's native syslog daemon.

    SECTION 6: Verify syslog path is live — firewall drops appearing in Loki (TEL-04, TEL-05)
    - Step 6.1: Wait 60 seconds for first syslog entries to arrive from IPFire
    - Step 6.2: Send a test FORWARDFW log line from dev machine (simulates IPFire drop):
        logger -n 192.168.1.101 -P 514 --udp "$(date '+%b %d %H:%M:%S') ipfire kernel: FORWARDFW DROP SRC=203.0.113.42 DST=192.168.1.100 PROTO=TCP DPT=22"
    - Step 6.3: Query Loki for the test entry (within 30 seconds):
        curl -s -G 'http://192.168.1.101:3100/loki/api/v1/query_range' \
          --data-urlencode 'query={job="ipfire-syslog"} |= "FORWARDFW"' \
          --data-urlencode 'limit=3' | jq '.data.result[0].values[-1][1]'
        Expected: JSON string containing "FORWARDFW" log entry
        If null: check Alloy debug UI at http://192.168.1.101:12345 — verify loki.source.syslog component shows received events
    - Step 6.4: Also verify from Grafana:
        Open http://192.168.1.101:3000 > Explore > Select Loki > Run query: {job="ipfire-syslog"}
        Expected: log entries from IPFire within last 5 minutes

    SECTION 7: Run validate-phase5.sh (TEL-01 through TEL-08)
    - Step 7.1: Run quick suite from supportTAK-server:
        ssh opsadmin@192.168.1.101 'bash /opt/telemetry/scripts/validate-phase5.sh'
        Expected: TEL-01 through TEL-08 all PASS or SKIP (DASH-01, DASH-03, DASH-04 may show SKIP — acceptable at this stage)
        TEL-07 SKIP is always acceptable (ordering is procedural)
    - Step 7.2: If any FAIL, check the specific section message and resolve before proceeding to Plan 03.
    - Acceptable state to proceed to Plan 03: TEL-01 PASS, TEL-02 PASS, TEL-03 PASS, TEL-04 PASS, TEL-05 PASS, TEL-06 PASS, TEL-07 SKIP, TEL-08 PASS

    SIGN-OFF CHECKLIST (requirements covered by this plan):
    - [ ] TEL-01: IPFire syslog forwarding configured in WUI (Logs > Log Settings → 192.168.1.101)
    - [ ] TEL-02: UDP port 514 available on supportTAK-server (no rsyslog conflict)
    - [ ] TEL-03: All 5 Docker containers running (loki, alloy, prometheus, node-exporter, grafana)
    - [ ] TEL-04: Alloy receiving syslog; Loki has {job="ipfire-syslog"} entries
    - [ ] TEL-05: Loki /ready returns "ready"
    - [ ] TEL-06: Grafana accessible at :3000, Loki datasource listed in API
    - [ ] TEL-07: SKIP — phased ingest order is procedural (syslog first, EVE JSON in Plan 03)
    - [ ] TEL-08: loki-config.yml has retention_period: 720h

    ---

    After creating the file, commit:
    git add docs/telemetry-deployment-runbook.md
    git commit -m "feat(05-02): add telemetry-deployment-runbook.md"
  </action>
  <verify>
    <automated>
      bash -c "test -f C:/Users/ablan/Firewall/docs/telemetry-deployment-runbook.md && echo 'runbook: OK' || echo 'MISSING'"
      bash -c "grep -q '192.168.1.101' C:/Users/ablan/Firewall/docs/telemetry-deployment-runbook.md && echo 'host IP: OK'"
      bash -c "grep -q 'rsyslog' C:/Users/ablan/Firewall/docs/telemetry-deployment-runbook.md && echo 'rsyslog conflict section: OK'"
      bash -c "grep -q 'FORWARDFW' C:/Users/ablan/Firewall/docs/telemetry-deployment-runbook.md && echo 'FORWARDFW test: OK'"
      bash -c "grep -c 'TEL-0' C:/Users/ablan/Firewall/docs/telemetry-deployment-runbook.md"
    </automated>
  </verify>
  <acceptance_criteria>
    - docs/telemetry-deployment-runbook.md exists with 7 sections
    - Contains IP 192.168.1.101 (telemetry host)
    - Contains rsyslog conflict check (Section 2)
    - Contains FORWARDFW test log line injection command (Section 6)
    - Contains sign-off checklist covering TEL-01 through TEL-08
    - Committed to git under feat(05-02)
  </acceptance_criteria>
  <done>telemetry-deployment-runbook.md is committed. Human deployer has a complete ordered procedure for standing up the stack and verifying the syslog path is live before proceeding.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 (Claude) created telemetry-deployment-runbook.md. This checkpoint deploys the stack using that runbook and verifies the syslog path is live.

    Claude cannot perform this checkpoint — it requires:
    1. SSH to supportTAK-server (192.168.1.101) to deploy Docker Compose stack
    2. WUI access to IPFire (https://192.168.1.1:444) to configure syslog forwarding
    3. Live network verification that IPFire logs are appearing in Loki
  </what-built>
  <how-to-verify>
    Follow the telemetry-deployment-runbook.md (docs/telemetry-deployment-runbook.md) in order.

    Minimum acceptance steps for this checkpoint:

    1. Deploy stack to supportTAK-server:
       scp -r C:/Users/ablan/Firewall/telemetry/ opsadmin@192.168.1.101:/opt/
       ssh opsadmin@192.168.1.101 'docker compose -f /opt/telemetry/docker-compose.yml up -d'
       ssh opsadmin@192.168.1.101 'docker compose -f /opt/telemetry/docker-compose.yml ps'
       Expected: 5 containers running

    2. Verify Loki ready:
       curl -s http://192.168.1.101:3100/ready
       Expected: "ready"

    3. Configure IPFire syslog forwarding via WUI:
       https://192.168.1.1:444 > Logs > Log Settings
       Set Syslog server = 192.168.1.101, UDP, Save

    4. Wait 60 seconds, then query Loki:
       curl -s -G 'http://192.168.1.101:3100/loki/api/v1/query' \
         --data-urlencode 'query=count_over_time({job="ipfire-syslog"}[5m])' | jq '.'
       Expected: result array is non-empty

    5. Run validate-phase5.sh:
       scp C:/Users/ablan/Firewall/scripts/validate-phase5.sh opsadmin@192.168.1.101:/opt/telemetry/scripts/
       ssh opsadmin@192.168.1.101 'sed -i "s/\r$//" /opt/telemetry/scripts/validate-phase5.sh && chmod +x /opt/telemetry/scripts/validate-phase5.sh'
       ssh opsadmin@192.168.1.101 'bash /opt/telemetry/scripts/validate-phase5.sh'
       Expected: TEL-01 through TEL-08 all PASS or SKIP (TEL-07 SKIP is acceptable)

    6. Verify Grafana UI (http://192.168.1.101:3000) shows Loki datasource connected.

    If rsyslog conflict blocks Alloy from binding port 514:
       ssh opsadmin@192.168.1.101 'sudo systemctl stop rsyslog && sudo systemctl disable rsyslog'
       ssh opsadmin@192.168.1.101 'docker compose -f /opt/telemetry/docker-compose.yml restart alloy'
  </how-to-verify>
  <resume-signal>
    Type "syslog-live" when validate-phase5.sh shows TEL-01 through TEL-08 passing (TEL-07 SKIP acceptable).
    Or describe any failures if stack deployment encountered issues.
  </resume-signal>
</task>

</tasks>

<verification>
After human checkpoint approves:

```bash
# Stack health from supportTAK-server
ssh opsadmin@192.168.1.101 'docker compose -f /opt/telemetry/docker-compose.yml ps'
# Expected: 5 containers running

# Loki ready
curl -s http://192.168.1.101:3100/ready
# Expected: ready

# Syslog entries in Loki
curl -s -G 'http://192.168.1.101:3100/loki/api/v1/query' \
  --data-urlencode 'query=count_over_time({job="ipfire-syslog"}[1h])' | jq '.data.result | length'
# Expected: > 0

# validate-phase5.sh TEL-01 through TEL-08
ssh opsadmin@192.168.1.101 'bash /opt/telemetry/scripts/validate-phase5.sh'
# Expected: ALL CHECKS PASS or COMPLETE with 0 FAIL
```
</verification>

<success_criteria>
- docs/telemetry-deployment-runbook.md committed to repo
- Docker Compose stack running on 192.168.1.101 with 5 containers
- Loki /ready returns "ready"
- IPFire syslog configured to forward to 192.168.1.101 UDP 514
- Loki has entries in {job="ipfire-syslog"} stream
- validate-phase5.sh TEL-01 through TEL-08: all PASS or SKIP (zero FAIL)
</success_criteria>

<output>
After completion, create `.planning/phases/05-telemetry-pipeline-and-dashboards/05-02-SUMMARY.md` covering:
- runbook sections created and key deployment steps
- Stack deployment outcome (any issues encountered, workarounds applied)
- IPFire syslog forwarding verification result
- validate-phase5.sh results (exact output)
- What Plan 03 uses from this plan (live Loki instance, Alloy running, /var/log/ipfire-eve/ directory exists)
</output>
